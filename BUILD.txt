Build tag: nrs-graphs-and-week6-video

CHANGES SINCE goal-video-capture
1. One video per cycle: the optional video is now offered at a SINGLE
   check-in (week 6) instead of the 6–8 window, so there's at most one per
   cycle. The week is one line in app/[locale]/checkin/page.tsx
   (prompt.weekNumber === 6) — change the 6 to 7 or 8 if you prefer.
2. NRS goals are now graphed on a 0–10 NRS chart; GAS goals keep the
   −2..+2 banded GAS chart. The clinician already chooses NRS or GAS when
   creating a goal; each goal now shows on its matching graph.
   - NRS chart: 0–10 axis with gridlines at 0/5/10, the raw weekly value
     plotted (no conversion to GAS). Tapping a week shows "NRS x/10".
   - GAS chart: unchanged (five directional bands, levels −2..+2).
   - Applies everywhere the chart appears: clinician patient page, the
     enlarged modal, and the physiotherapist patient page.
   No data change — the raw 0–10 value was already stored per week, so this
   is a display change only.

MIGRATIONS
- No NEW migration in this build. 0062_goal_video.sql (from the previous
  build) is unchanged and is still required if you haven't run it yet
  (it adds the video columns, RPCs, and the goal-videos Storage bucket).

DEPLOY
- Upload this zip to GitHub -> Vercel as usual. If 0062 hasn't been run,
  run it in the Supabase SQL editor first (see that file's header).

VERIFY ON SCREEN (I can't see rendered output)
- Open a patient with an NRS goal that has weekly check-ins: confirm the
  chart shows a 0–10 axis and plots the raw values, and that tapping a week
  reads "NRS x/10" (no GAS line). Confirm a GAS goal still shows the banded
  −2..+2 chart. Check both the inline chart and the enlarged (tap-to-expand)
  modal, and the physiotherapist view.
- Still pending from before: clinician PLAYBACK of recorded videos (next
  slice), and on-device testing of video capture/upload.

How to confirm THIS build is deployed:
- BUILD.txt tag "nrs-graphs-and-week6-video".
- An NRS goal's progress chart shows a 0–10 axis (not −2..+2 bands).
