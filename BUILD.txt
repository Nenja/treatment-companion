BUILD: simplify-cockpit-5   (#6 overlap fix + goal 'Edit' button regroup — NO migration)
==========================================================================
MIGRATION: none. DB stays at 0088. Nothing to run in Supabase.
Batch 5 of cockpit simplification. Backlog + status in HANDOVER §8.

DONE
- #6 OVERLAP FIXED. The stray "Weekly ratings data for … / Week / Rating" text
  under each goal graph was a screen-reader data table I added in audit-fixes
  (GoalProgressView, className="sr-only"). .sr-only is present in the built CSS
  but was NOT hiding it in production, so it rendered visibly and collided with
  "Tap a point for details." Removed the table + its tableId + the svg's
  aria-describedby. The chart keeps its descriptive aria-label (basic a11y).
- GOAL-CARD BUTTON: "Recalibrate" -> "Edit", regrouped. editGoalCta and the
  EditGoalDrawer eyebrow/title renamed Recalibrate->Edit (en+da). The Edit
  button moved from its own row above the chart into the goal action row, next
  to the video-task button and Retire (now: Edit · video · Retire).

VERIFIED LOCALLY
- tsc --noEmit clean.
- Production build OK via the font-stub procedure (restored byte-for-byte, zero
  BUILD-STUB remnants). 60/60 pages.

DEPLOY: no migration; unzip, push, Vercel builds. Page count 60.

** QA **
- Under each goal graph: NO stray "Weekly ratings data…/Week/Rating" text; the
  "Tap a point for details" caption sits alone.
- Goal action row reads: Edit · (Record/Set baseline + video task) · Retire.
  The Edit button (pencil) opens the same edit drawer as before.

BACKLOG REMAINING (HANDOVER §8): #5 move video-task config under Edit (drop the
separate video button); #9b relocate therapist input to the treatment page;
#8 show-last-treatment dialog; #11 gate handoff note on therapist activity.
(#4 muscle mapping parked.)
