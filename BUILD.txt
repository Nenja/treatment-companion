Build tag: training-home-vs-therapist

CHANGES SINCE clinician-training-overview
1. Training is now split into "At home" vs "With a therapist".
   - Check-in: the training step now has TWO Mon–Sun selectors — "At home"
     and "With a therapist" (each optional; empty = none).
   - Stored on the check-in: training_days (home, existing) and the new
     training_days_therapist. A day can be in both.
2. The clinician "Training" overview (active-cycle patient page) is now
   COLLAPSIBLE. The header stays visible with the summary
   ("home X/wk · therapist N×") and a chevron; the grid collapses.
   On the grid: a filled sage cell = trained at home that day; an amber ring
   = a session with a therapist that day (a cell can be both). Legend updated.

DATABASE — NEW MIGRATION (must run)
- supabase/migrations/0064_training_with_therapist.sql (also attached):
    * adds weekly_checkin.training_days_therapist
    * replaces set_checkin_training_days with a two-array version
      (home + therapist; therapist defaults null). Owner-checked, validates 1..7.
- Run it in the Supabase SQL editor, then deploy this code. (0062 and 0063
  from earlier builds must also be applied.)

VERIFY ON SCREEN (I can't see rendered output)
- Do a check-in: the training step shows two day selectors (home + therapist).
  Tick some in each, submit. On the clinician patient page, the "Training"
  card collapses/expands, and the grid shows home (filled) and therapist
  (amber ring) for those days.

How to confirm THIS build is deployed:
- BUILD.txt tag "training-home-vs-therapist".
- The check-in training step has "At home" and "With a therapist" selectors.
