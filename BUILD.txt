BUILD - sv-nb-reminders-1   (CUMULATIVE)
Swedish + Norwegian for the check-in reminder push, plus a crash-safety fix in
the same code. Carries everything still uncommitted.

>>> CUMULATIVE — one drop-in deploys everything. <<<

WHAT'S NEW THIS BATCH (one file)
  supabase/functions/send-checkin-notifications/index.ts
    - Added sv + nb text for BOTH notifications (the "due now" one and the late
      reminder). First pass — please have a native speaker check the wording.
    - Fixed a latent crash: the code picked the message text by the subscriber's
      language with NO fallback, so a Swedish/Norwegian patient (those languages
      were added recently) would have caused that send to error. It now falls
      back to English for any unexpected language.

>> IMPORTANT — THIS FILE IS NOT DEPLOYED BY GITHUB / VERCEL. <<
   The reminder sender is an Edge Function you deploy SEPARATELY in Supabase.
   For the new text to take effect, re-deploy `send-checkin-notifications`
   (Supabase dashboard -> Edge Functions, or the CLI). Keeping it in the repo
   keeps one source of truth, but the deploy step is manual.

SWEDISH / NORWEGIAN TEXT ADDED (for your reviewer)
  Initial  sv: "Veckovis status" / "Hur har veckan gått? Tryck för att skatta dina mål."
           nb: "Ukentlig status" / "Hvordan har uken gått? Trykk for å vurdere målene dine."
  Reminder sv: "Påminnelse" / "Du har en status som väntar. Det tar ungefär två minuter."
           nb: "Påminnelse" / "Du har en status som venter. Det tar omtrent to minutter."

CARRIED (cumulative, unchanged): the full Vitest harness (now 35 tests), the
chart consolidation, and the guarded migration 0104.

VERIFIED: app type-check clean, i18n parity OK, 35/35 unit tests pass. The edge
function is Deno (not in the app's type-checker), so its changed text + fallback
logic were type-checked in isolation instead.

STILL TO RUN IN SUPABASE (unchanged): 0103, 0104 (re-run; guarded), 0105, 0106.

DEPLOY: web files -> Commit -> Push (Vercel).  Edge function -> re-deploy in Supabase.
