BUILD - consent-consolidation-1   (CUMULATIVE)
Consent now lives in ONE place. The toolbar's "Video" action is renamed
"Consent"; the panel holds clinical / educational / research consent; the
consent rows are removed from the Background card.

>>> CUMULATIVE — this one drop-in also carries the previous batch
>>> (suggestion-approve-redesign-1) and its migration 0107. <<<

WHAT'S IN THIS DROP
  This batch (consent):
    components/clinician/ClinicianVideoModal.tsx   (now the Consent panel)
    components/clinician/BackgroundCard.tsx        (consent rows removed)
    components/clinician/PatientActionRow.tsx      (Video icon -> shield-check)
    app/[locale]/clinician/patient/page.tsx        (wiring + relabel)
    messages/en|da|sv|nb.json                      (+clinicalConsent; relabels)
  Carried from suggestion-approve-redesign-1:
    supabase/migrations/0107_combine_suggestion_into_goal.sql   (MIGRATION)
    lib/supabase/clinicianPatient.ts               (combine hook)
    app/[locale]/clinician/suggestion/page.tsx     (Approve / Set aside redesign)

WHAT CHANGED, IN PLAIN TERMS
  - The "Video" button in the patient toolbar is now "Consent" (shield icon).
  - Opening it shows three consent toggles, each with a status pill:
      * Recording for clinical use
      * Educational use of video
      * Research participation  (keeps the withdraw-confirmation)
  - The Background card no longer shows any consent — it's only demographics,
    treatment, medication, and devices now. Consent lives ONLY in the panel.
  - (Per-goal video recording is unchanged; it still reads the same consent.)

DEPLOY — IN ORDER
  1. Supabase SQL editor: run (if you haven't already, from the last drop)
         supabase/migrations/0107_combine_suggestion_into_goal.sql
     (Idempotent — safe to run again. This is the only migration in the drop.)
  2. GitHub Desktop: drop all files from this zip into the repo, same paths.
     DO NOT overwrite root package.json / package-lock.json (untouched here).
  3. Commit + push. Vercel auto-builds.

VERIFICATION DONE FOR YOU (sandbox)
  - Production build: 110/110 pages, exit 0.
  - TypeScript: clean (0 errors).
  - Unit/component tests: 41/41 pass.
  - i18n parity: 1668 keys, en/da aligned (sv/nb same key set).
  - (Migration 0107 was verified on a throwaway Postgres in the last batch:
     focused harness + all four guards + full 0001->0107 chain, 0 failures.)

WHAT I CANNOT VERIFY (please click through once after deploy)
  - The rendered Consent panel in Firefox (three pill+toggle rows, the new
    icon, the relabeled toolbar button).
  - Live toggling against the real database (clinical/educational via one
    mutation; research via its own, including the withdraw confirmation).
  - sv/nb wording quality (first pass — flag for a native speaker).

NEXT IN THIS THREAD
  Playwright E2E smoke (login -> check-in) — the last roadmap item. It will be
  a scaffold + run instructions; it cannot be proven green from the sandbox.
