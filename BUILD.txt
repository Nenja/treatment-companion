BUILD - physio-progress-retire-1   (CUMULATIVE)
Tiny tidy on top of redcap-export-1: the orphaned /physio/progress route is
now a redirect. Everything else in this zip is unchanged from redcap-export-1.

>>> CUMULATIVE — supersedes redcap-export-1 <<<
One drop-in deploys it all. If you already deployed redcap-export-1, the only
new file here is app/[locale]/physio/progress/page.tsx.

WHAT CHANGED
- /physio/progress no longer renders its own progress form (that form has
  lived inline on /physio/patient since cockpit-65). The route is now a
  locale-preserving redirect to /physio/patient, so any old bookmark/link
  still lands on the live cockpit. /physio/patient handles auth + session.
  Build 110/110, tsc clean.

STILL TO RUN IN SUPABASE (unchanged from redcap-export-1)
- Migrations 0103, 0104, 0105, 0106 (run any not yet applied).
- See redcap-export-1 / reminder-cron notes for the edge-function deploy +
  Vault secrets if reminders aren't live yet.

DEPLOY
  Copy files into the repo (keep folders) -> Commit -> Push (Vercel builds).
  Do NOT run npm install or overwrite package.json / package-lock.json.
