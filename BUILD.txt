BUILD — onboarding-copy-1
Patient onboarding / help copy refresh (WEB ONLY)

WHAT THIS IS
Updates to the patient-facing help text and the onboarding wizard so they
match what the app does now. English + Danish (Danish is a first pass —
please have a native speaker review it). No migration, no new dependencies.
Verified: build 62/62, TypeScript clean.

HOW TO DEPLOY (GitHub Desktop -> Vercel)
1. Unzip. Copy the 3 changed files into your repo (keep folder structure),
   plus the updated HANDOVER.md / BUILD.txt / TRANSFER_PROMPT.md.
2. GitHub Desktop: review, Commit, Push. Vercel rebuilds.

DO NOT: run "npm install", run any SQL, or overwrite package.json/lock.

THE 3 CHANGED FILES
- messages/en.json
- messages/da.json
- app/[locale]/goals/page.tsx

WHAT CHANGED
1. The "?" help on the patient HOME now describes the whole screen — the
   weekly check-in, the "Your goals" row, "Show visit code", and notes from
   the care team. (Before, it mentioned only check-ins.)
2. The GOALS page now has its own "?" help (it had been showing the home
   screen's help by mistake).
3. The first screen of the patient SETUP WIZARD now mentions that the home
   screen also shows their goals and any notes from the care team.

PLEASE EYEBALL ON YOUR PHONE
- Patient home -> "?": reads correctly, covers goals / visit code /
  care-team notes.
- Goals page -> "?": now describes the goals page (graphs, suggesting a
  goal), not the home screen.
- Replay the patient tutorial (account menu) -> first step mentions goals
  + care-team notes.
- Danish wording on all of the above — please review as a native speaker.

COMING NEXT
Swedish + Norwegian (Bokmal) as selectable languages: full first-pass
translation files + routing, offered in the login/profile language picker
(Danish-style first pass, flagged for native review).
