BUILD: simplify-cockpit-23   (demographics line moved under the patient name — NO migration)
==========================================================================
MIGRATION: none. DB stays at 0090. Nothing to run in Supabase.

DONE
- The patient demographics line ("41 years · Traumatic brain injury · Left ·
  With aid") was floating in the body context line after the Option A
  restructure — disconnected from the name (in the header) and the card below,
  so it read as orphaned. The header comment already said the summary should
  "sit on its own line beneath the name", so this restores that:
  * The demographics summary now renders as a sub-line directly UNDER the
    patient name in the page header.
  * PatientBanner is now just the modality pill (the summary prop was removed).
    The lone pill still fills the wide-layout top-alignment band, so the
    "since last visit" / first-graph alignment from build 22 is preserved.
- No new i18n keys.

VERIFIED LOCALLY
- tsc --noEmit clean.
- Production build OK via the font-stub procedure. 60/60 pages.

DEPLOY: no migration; unzip / push / Vercel builds. Page count 60.

** QA (desktop) **
- The demographics line sits right under the patient name in the header.
- The body context column shows only the "Botulinum toxin" pill above
  "since last visit".
- The "since last visit" card top still lines up with the first goal graph.
