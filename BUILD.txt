BUILD - admin-tool-rail-1   (Admin entry in the patient Tools rail)

WHAT THIS IS
  Cumulative snapshot (deploy THIS one). Migrations 0001-0111. UI only; no
  migration. Supersedes admin-button-actionrow-1 (that put the button on the
  home header by mistake — reverted here).

CONTAINS (cumulative)
  - Admin added to the patient-page Tools rail (this delivery).
  - Research export on the admin page; 0111 export fix; dev scenarios retired.

THIS CHANGE
  Added an "Admin" entry (gear icon) to the clinician patient Tools rail
  (the icon row with Training / Consent / History / Export), shown only to
  admins. Tapping it navigates to /clinician/admin (where the REDCap export +
  sync now live). It renders in all three rail variants (toolbar / sidebar /
  body row) via a new showAdmin prop on PatientActionRow, gated by
  profile.isAdmin at the three call sites.
  The clinician HOME page is back to its original state (bottom admin button) —
  the header-action-row version from the previous build was reverted.
  New i18n keys clinician.patient.actionAdmin / actionShortAdmin = "Admin"
  (en/da/sv/nb; same word in all). Removed the short-lived clinician.adminButton
  key. Parity now 1715 keys.
  Note: admin is a global tool, not patient-scoped — it sits in the rail only
  for discoverability; it navigates away from the patient view.

VERIFIED
  tsc clean; eslint 0 errors; check:i18n OK (1715 keys, en/da aligned);
  font-stub build compiled; layout.tsx SHA restored (d6901997...);
  0 BUILD-STUB remnants.

MIGRATIONS
  None new. (0111 already run in production.)
