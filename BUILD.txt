BUILD - docs-care-team-notes-decision-1   (record a resolved decision; docs only)

WHAT THIS IS
  Cumulative repo snapshot on top of hooks-refactor-1. DOCS ONLY — no app,
  migration, test, or config change. Nothing to run, nothing to QA.
  Migrations still 0001-0110.

WHAT CHANGED
  Records Nikolaj's decision (2026-06-16) on the care-team-notes finding from
  rls-denial-tests-1: patient-readable IS intended — these notes
  (treatment_handoff, goal_handoff_note, therapist_note) are the patient's own
  care record, and the patient may read the ones about themselves (never
  another patient's). Author-private notes are explicitly NOT wanted.

  This was already TRUE in the product (migration 0096 + the authoring UI hints
  "the patient can read it too" + the patient-facing "Notes from your care
  team" surface). Only the handoff docs were stale, still carrying the old
  "never patient-visible" framing and a "decision pending" flag. Updated:
    - HANDOVER.md: header blockquote, the principles line (~line 65), the §5.13
      correction, and the §7 finding — all now say "resolved / intended", and
      note no app change is needed.
    - TRANSFER_PROMPT.md: the "decision pending" line is now "resolved".

  No code path, RLS policy, or UI string changed — the implementation already
  matched the decision.

VERIFIED
  Docs only; tsc/lint/build unaffected from hooks-refactor-1.

MIGRATIONS
  Nothing new. If 0110_studies.sql is not yet applied, run it (copy in
  outputs). Otherwise no SQL to run.

NOTE ON THIS ZIP
  Full repo minus node_modules, .next, build artifacts, and .git.
