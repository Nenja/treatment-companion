BUILD - component-tests-1   (CUMULATIVE)
Adds the component-test layer to the harness and a first component test for the
patient's 0-10 rating control. 41 tests now pass (was 35).

>>> CUMULATIVE — one drop-in deploys everything. <<<

WHAT'S NEW THIS BATCH
  tests/GoalRatingPicker.test.tsx   (+6 tests, runs in jsdom)
  vitest.config.ts                  (React plugin + .tsx test discovery)
  package.json / package-lock.json  (test-tooling devDeps; see note below)

WHAT THE COMPONENT TESTS CHECK (GoalRatingPicker = the 0-10 tap scale)
  - renders 11 buttons (0-10) as one radio group
  - tapping a number calls onChange once with that number
  - exactly one button shows as selected for the current value
  - the "tap a number" hint shows only until a value is picked
  - the Best/Worst end labels follow the goal's direction (higher-is-better
    puts Best at 10; lower-is-better flips it)

DEPENDENCY NOTES (please read)
  - DEV-ONLY tooling added: @testing-library/react, @testing-library/dom,
    @testing-library/jest-dom, jsdom, @vitejs/plugin-react. None ships to
    production; the Vercel build is unaffected (verified: build still 110/110).
  - @types/node bumped 22.10.0 -> 22.19.21. Vitest 4 pulls in vite 8, which
    needs @types/node >= 22.12. Bumping keeps the lockfile clean for npm ci.
  - EASIEST: take the included package.json + package-lock.json as-is. Hand-
    diffing the new devDeps + the bump + ~800 lock lines is error-prone.
  - The `npm audit` warnings are all in the dev-tooling tree, not in anything
    that runs in production.

CARRIED (cumulative, unchanged): the sv/nb reminder localization + crash fix,
the rest of the Vitest harness (now 41 tests), the chart consolidation, and the
guarded migration 0104.

VERIFIED: build 110/110, tsc clean, i18n parity OK, 41/41 unit tests pass.

STILL TO RUN IN SUPABASE (unchanged): 0103, 0104 (re-run; guarded), 0105, 0106.
EDGE FUNCTION (unchanged): re-deploy send-checkin-notifications in Supabase.

DEPLOY: copy files into the repo (keep folders) -> Commit -> Push.
Do NOT run npm install yourself.
