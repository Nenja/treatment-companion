BUILD: simplify-cockpit-7   (drawer backdrop-close + background-field cleanup — NO migration)
==========================================================================
MIGRATION: none. DB stays at 0088. Nothing to run in Supabase.
Batch 7 of cockpit simplification. Backlog + status in HANDOVER §8.

DONE (refinements from your feedback)
- DRAWERS CLOSE ON BACKDROP CLICK. useModalA11y only handled Escape; added an
  onClick backdrop handler to CockpitPanelDrawer, RecordGoalDrawer,
  EditGoalDrawer, LastTreatmentModal, TreatedMusclesModal. (VideoProtocolEditor
  already closed on backdrop.) Click anywhere outside a drawer/modal to dismiss.
- PATIENT BACKGROUND FIELD (PatientBanner):
  * Removed the patient NAME restatement (it's already in the page header).
  * MEDICATION moved here: the field always shows the medication line with an
    Edit button (opens the medication drawer); the MEDICATION button was removed
    from the action row.

VERIFIED LOCALLY
- tsc --noEmit clean.
- en/da parity intact (reused existing labels; no new keys).
- Production build OK via the font-stub procedure (restored byte-for-byte, zero
  BUILD-STUB remnants). 60/60 pages.

DEPLOY: no migration; unzip, push, Vercel builds. Page count 60.

** QA **
- Open Training (or any) drawer -> click the dimmed area outside it -> it closes.
- Background field: shows demographics + modality + treatment date + medication
  (with Edit) + devices, and NO duplicated patient name. Edit opens the
  medication editor; Save/Cancel still work.
- Action row no longer has a Medication button.

NOT YET DONE (next batch)
- Therapist action button removal + #9b treatment-page button (a counted,
  Suggestions-style button that opens the therapist suggestions). Kept on the
  cockpit for now so therapist input isn't stranded.
- Training: make the day-list a pop-up instead of a collapsible-in-drawer.
- Goal graphs possibly too wide -> consider capping chart width (confirm first).
