BUILD: simplify-cockpit-85 — CLINICIAN-PAGE REFINEMENTS. Single "Mark considered" action for
therapist suggestions; handoff note de-cluttered; wrong "Therapist only" badge removed.
NO MIGRATION (app + i18n only).

DISCARD 21-84 (superseded). Keep this zip.

DB STATE — no new migration. Still run any PENDING ones in order:
  • 0090, 0095, 0096, 0097, 0098  (paste schema_audit.sql to see which are still MISSING)
  Note: the badge fix below assumes 0096 is/will be applied (it grants the patient read access
  to the care-team notes). The fix is correct regardless, but the patient only actually sees
  the note once 0096 is live.

WHAT CHANGED:
  1) Therapist-suggestion review — ONE action now.
       The two buttons "Mark considered" and "Dismiss" set different statuses but did the same
       thing. Collapsed to a single "Mark considered" (sets accepted/reviewed; item leaves the
       review queue, shows "Considered"). Done in all four places: both TherapistInputPanel
       widgets (goal + muscle) and both inline lists on the clinician patient/cockpit page.
       (The goal list's old "Accept" label is now "Mark considered" for consistency.)
       Old "dismissed" records still display "Dismissed". Dismiss i18n keys left in place.

  2) Physician -> therapist handoff note — just the written note.
       Dropped the "Did you change the treatment this visit?" (Adjusted / No change / Not
       specified) toggle. Save now always sends treatment_changed = null. The RPC and DB column
       are UNCHANGED (no migration) — old values preserved; new notes simply have no flag.

  3) Audience label fixed.
       Removed the "Therapist only" badge on that note. It contradicted the hint right below it,
       and was wrong: migration 0096 gives the patient read access to these care-team notes
       (their own care record). The hint ("...the patient can read it too") stays. Also fixed a
       stale "never patient-visible" code comment in GoalHandoffNotes.tsx. Removed the dead
       handoffChanged* / handoffAudienceBadge i18n keys (en + da).

  DIRECTION NOTE: I resolved the badge/hint contradiction toward "patient can read" because that
  is the deliberate 0096 design. If you meant these notes to be therapist-ONLY, that's the
  opposite change (drop the 0096 patient-read policies) — tell me and I'll flip it.

CANNOT BE VERIFIED HERE (your QA):
  • Native-Danish review (no new strings here, but the handoff hint wording is worth a glance).
  • That the suggestion queue + receipts read well with a single action on real screens.

BUILD VERIFICATION (this end): next build -> 62/62; tsc --noEmit clean; i18n parity en/da equal.

DEPLOY: unzip -> GitHub -> Vercel. No SQL for this build (beyond any still-pending 0090-0098).
