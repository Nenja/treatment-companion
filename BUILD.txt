BUILD: simplify-cockpit-73 — therapist trend-chart size fix.
DEPLOY THIS ZIP. DISCARD 21-72 and earlier.

MIGRATION: none new. Run 0095_therapist_note if you haven't.

WHAT CHANGED (cockpit-73)
- The per-goal trend chart on the therapist page was rendering far too tall (~280px) in the
  wide right column. It now renders as a short, full-width trend strip (~180px). Same data,
  same 0-10 / GAS layout, no distortion. The clinician treatment-page chart is unchanged.

DEPLOY: unzip, push to GitHub, Vercel auto-builds. (Still outstanding, cockpit-57: deploy
the send-checkin-notifications Edge Function.)

REMINDER: make sure the deploy actually lands — the live site had been stuck on a pre-cockpit-70
build. After pushing, confirm Vercel ran a fresh build and hard-refresh Firefox (Ctrl+Shift+R).

QA (cannot verify here)
[ ] Therapist page: each goal's chart is a short, wide strip, not a tall block.
[ ] Clinician treatment page: chart looks exactly as before.

VERIFIED IN BUILD: tsc clean · next build 62/62 · i18n en/da parity 1568 keys · font-stub restored.
