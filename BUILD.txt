BUILD: simplify-cockpit-2   (read-aloud bug fix + night-mode investigation + #4 draft — NO migration)
==========================================================================
MIGRATION: none. DB stays at 0088. Nothing to run in Supabase.
Batch 2 of the cockpit-simplification effort. Backlog + status in HANDOVER §8.

DONE
- #2a READ-ALOUD FIXED. The toggle wrote read_aloud to the DB but called
  invalidateQueries(['auth']) — and the profile lives in AuthProvider's
  useState, not a react-query cache, so that was a no-op: the preference never
  refreshed in memory and the speaker button never appeared until a full
  reload. Now uses refreshProfile() like the palette/night setters. One file:
  lib/supabase/readAloud.ts.

INVESTIGATED, NO FIX YET (need input)
- #3 NIGHT-MODE. Traced the whole path: night_mode round-trips through the
  profile correctly; resolveColors returns proper dark palettes; the compiled
  utilities use var(--color-*) (verified .bg-cream{background-color:var(--color-cream)}
  in the built CSS, so runtime overrides DO apply); no stray non-token colours
  on the main surfaces. I could NOT reproduce "doesn't work properly" from the
  code. Please tell me the exact symptom:
    (a) nothing visibly changes when you toggle, or
    (b) most of the app changes but some areas stay light, or
    (c) it changes then reverts / flashes light on reload.
  Each points to a different fix.

DRAFT FOR YOUR REVIEW
- #4 muscle -> patient-function mapping: docs/muscle-function-mapping-DRAFT.md.
  Finding: body muscles are FREE TEXT (no catalogue), so this needs a structured
  muscle catalogue with patient-function labels. The draft is that catalogue's
  first cut (upper/lower limb, en + da). Please correct the function column;
  then I'll build the picker + switch the patient "treated muscles" pop-up to
  show the function (with a fallback to raw text for legacy entries).

VERIFIED LOCALLY
- tsc --noEmit clean.
- Production build OK via the font-stub procedure (restored byte-for-byte, zero
  BUILD-STUB remnants). 60/60 pages.

DEPLOY: no migration; unzip, push, Vercel builds. Page count 60.

** QA **
- Turn read-aloud ON in a PATIENT's Appearance settings, then look at patient
  text: the speaker button should appear immediately, no reload. Tap it: the
  device reads the text (on-device speech; needs a browser/OS voice installed).

BACKLOG REMAINING (HANDOVER §8): #3 (awaiting symptom), #4 (awaiting your
markup), #5 video-under-Edit, #6 overlap, #8 show-last-treatment dialog,
#9 action icons -> side drawer + therapist input to treatment page,
#10 medication non-intrusive, #11 gate handoff note on therapist activity.
