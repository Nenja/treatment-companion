Build tag: meds-to-actionrow
Generated: 2026-06-02T06:59Z

What changed (vs "trim-header-and-meds"):
- Clinician patient page (logged into a patient):
  * Patient suggestions moved OUT of the action row to sit beside
    "Record a goal" (same panel, same review flow, keeps its count badge).
  * Anti-spastic medication added to the action row as a new icon (pill),
    opening an inline panel with the same read/Edit/Save UI it had on the
    treatment page. Saves via set_patient_medication.
  * Action row still has 4 icons: Medication, Therapist input, History, Export.
- Treatment page: medication card stays removed (from previous build).
- Face module, read-path, exports, header, wordmark: unchanged.

How to confirm THIS build is deployed:
- BUILD.txt at repo root says tag "meds-to-actionrow".
- On the patient page the action row's first icon is "Medication" (pill), NOT
  "Suggestions"; the Suggestions button sits next to "Record a goal".
