BUILD: simplify-cockpit-40   (NO migration; DB 0093; guided enable->consent->baseline flow: enabling video on a goal opens a 2-step follow-up — consent (gates recording) then film baseline.)
==========================================================================
WHY CUMULATIVE: the build sandbox reset itself between builds last session, so
the zips tagged 21-26 were each built on a reverted base and are NOT cumulative
(each missing pieces of the others). This zip re-applies the full intended set
onto the known-good build-20 base in one pass.

** DEPLOY THIS ZIP. DISCARD 21-31. **  Video chip moved into the goal-card header; video-delete is now a visible button in the player (open a "Since last visit" video -> Delete).

WHAT'S IN IT
1. "What's still needed" checklist on new-goal + approve calibration forms,
   shown from the start (no "started" gate).
2. "Video task" chip on video-enabled goal cards (baseline set/needed).
3. Compact layout option hidden for CLINICIANS (forced wide; toggle hidden in
   account menu + onboarding). Therapists unchanged.
4. Top alignment: empty lg:min-h-[39px] band keeps "since last visit" level with
   the first goal graph.
5. "Since last visit" lists each goal's MAX EFFECT only (goal name + one value;
   GAS shows +/-2). No most-recent, no improved/declined verdict.
6. Goal graphs stay clean (no value labels on the line).
7. New Background card below "since last visit": demographics + treatment type +
   medication. Demographics left the header, the modality pill left the top of
   the column, medication left the visit footer -- all consolidated here.

VERIFIED LOCALLY
- tsc --noEmit clean.
- i18n parity: 0 key mismatches, 0 ICU mismatches.
- Production build OK via the font-stub procedure. 60/60 pages.

** QA **
- As a clinician: account menu has no wide/compact toggle; cockpit always
  two-pane. As a therapist: the toggle still appears.
- Each video-enabled goal card shows a "Video task" chip.
- Open "Record a goal" with nothing filled: the checklist shows immediately.
- "Since last visit" shows goal name + max effect only; graphs have no labels.
- A "Background" card sits below "since last visit" with demographics, treatment
  type and medication (with Edit).
