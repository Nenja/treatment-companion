BUILD: simplify-cockpit-82 — diagnosis capture completed (added missing 'anoxic' etiology value) and surfaced in the clinician history header. Fixes a cockpit-81 medication-column bug. Migration 0097.

DISCARD 21-81 (superseded). Keep this zip.

DB STATE — RUN THESE IN SUPABASE IF NOT DONE (in order):
  • 0095_therapist_note.sql           (still pending on your side)
  • 0096_patient_care_team_notes.sql  (still pending on your side)
  • 0097_etiology_anoxic.sql          (NEW — run on its own; it's an enum ALTER TYPE)

WHAT CHANGED:
  • MIGRATION 0097: alter type etiology add value if not exists 'anoxic';
      Adds anoxic / hypoxic brain injury to the etiology picklist. Idempotent, no data change.
      Run it as its own statement (enum add-value shouldn't share a transaction).
  • Diagnosis capture ALREADY existed (etiology + etiology_detail on patient, written by
      set_patient_info, edited in the patient-info form). This build only filled the one
      missing picklist value and the read/display path — no new columns.
  • App: Etiology type + ETIOLOGY_VALUES gain 'anoxic' (placed before 'Other');
      etiology i18n label added (en + da, first-pass Danish).
  • History header now shows a DIAGNOSIS pill (etiology + optional detail) when present.
  • BUGFIX (from cockpit-81): usePatientHistory read 'current_antispastic_medication',
      which migration 0061 renamed to 'current_medication'. The med pill would have been
      blank. Fixed; usePatientHistory now also returns etiology + etiologyDetail.

VERIFIED: next build 62/62 · tsc --noEmit clean · i18n parity 1611/1611.

CANNOT verify from here (QA on your side):
  • Saving 'anoxic' in the patient-info form and seeing it persist.
  • The diagnosis pill rendering in history against real data.
  • Dark-mode contrast of the sage diagnosis pill.

DEPLOY: run 0097 in Supabase → unzip → push to GitHub → Vercel auto-builds →
        hard-refresh Firefox (Ctrl+Shift+R).
