BUILD: patient-visit-and-status   (patient cold-start #3 + #5)
=========================================================================
NO MIGRATION — client-only. (Requires 0081 from the previous build.)

WHAT THIS BUILD DOES
- #3 Teach the visit code:
  - A new "At your appointment" step in the patient onboarding wizard, with a
    small visit-code illustration, explaining: show/read the code so the
    clinician can open your record.
  - A "At your visit — Show visit code" card on the patient no-cycle home,
    linking to the visit-code screen. The empty state now explains the next
    real step instead of just "wait".
- #5 Suggestion status echo:
  - The patient home now shows a quiet "N goal(s) sent — your clinician will
    review them at your next visit" line whenever the patient has suggestions
    awaiting review. Closes the loop without any clinic->patient messaging.

DEPLOY
- Unzip; push to GitHub; Vercel builds. Page count 60. No SQL.

** QA (please verify — can't be tested here) **
- Replay patient onboarding: an "At your appointment" step appears between the
  check-in and comfort steps.
- The no-cycle home shows the visit-code card and it routes to /visit-code.
- After suggesting a goal, the home shows the "sent — your clinician will
  review" line (correct singular/plural), and it clears once the clinician
  approves or declines the suggestion.

NEXT IN THIS BATCH
- #4 check-in edit/undo window.
