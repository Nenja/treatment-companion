BUILD TAG: batch-b
=====================================================================

WHAT CHANGED — localization sweep (from the page-by-page review)
---------------------------------------------------------------------
Closed the remaining hardcoded-English gaps so the app is consistently
bilingual (en/da) for patients and staff. NO database / RPC / migration
change, and NO behaviour change — text only (plus two reworded strings,
see below).

Patient-facing:
- Home: the "could not load" error card and the "no treatment cycle yet"
  empty state.
- Visit code: "Generating..." / "No active code" and the generate-error
  message.
- Check-in GAS goals: the five level meanings ("Much better than
  expected" ... "Much less than expected") + the picker prompt, now
  localized in BOTH the rating buttons and the summary review. (Shared
  keys under patient.checkin.)

Clinician / physiotherapist-facing:
- The entire PHYSIO unlock screen (heading, instructions, "Visit code"
  label, "Unlock" button) — it was fully hardcoded English.
- New-goal: the measurement-model toggle (question, help, both option
  labels + descriptions) and the "NRS rating setup" heading.
- Suggestion approval: the "NRS rating setup" heading + 0-10 intro line.
- Clinician home: the "Admin: create or list accounts" link.
- Admin: the created-account confirmation block (account created / role /
  copy / dismiss).

Two now-stale strings reworded (they still described setting GAS
cut-offs, which batch-a removed): the new-goal rating intro and the
suggestion NRS intro no longer mention "mapping" a 0-10 answer to a GAS
level.

Intentionally NOT translated: /privacy. It is an acknowledged
pilot-stage placeholder pending real GDPR / Danish legal copy; it should
be written/translated properly when the real policy exists, not now.


DEPLOY STEPS
---------------------------------------------------------------------
1. Push the repo to GitHub -> Vercel as usual. No env changes.
2. DB: nothing new. Migrations are still through 0067. If you have not
   yet run 0067 (from batch-a, for GAS suggestion-approval), run it now:
       supabase/migrations/0067_approve_suggestion_gas.sql


HOW TO CONFIRM
---------------------------------------------------------------------
- Switch the app to Danish and spot-check the screens above; switch back
  to English and confirm wording is unchanged from before.


VISUAL QA FLAGS (could not be rendered in the build sandbox)
---------------------------------------------------------------------
[ ] In Danish, eyeball: patient home error + no-cycle states; the
    visit-code screen; the check-in GAS rating buttons + the summary
    line; the physiotherapist unlock screen; new-goal's model toggle.
[ ] Confirm no Danish string overflows its button/card (Danish runs a
    bit longer than English in a few of these).


BUILD VERIFICATION
---------------------------------------------------------------------
- next build: exit 0, "Generating static pages (58/58)".
- npx tsc --noEmit: exit 0.
- en/da translation parity: 1019 == 1019 keys, no diffs.
- Migrations unchanged (through 0067).
