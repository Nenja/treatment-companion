BUILD - checkin-e2e-fix-2  (test-only)
Follow-up that fixes the LAST remaining check-in test failure. The
signed-out->/login app guard is already live, so this drop is just the test.

WHAT'S IN THIS DROP
  e2e/smoke.spec.ts   (check-in walk reworked to handle the submit step)

WHAT WAS WRONG THIS TIME
  After the previous fix, the rating registered fine — but the test then failed
  with "button not found". On the last step, clicking "Send my check-in" submits
  and the screen switches to the "Thank you" page, so the button disappears. The
  loop took one more lap and tried to use that now-gone button.

WHAT THE TEST DOES NOW
  Every step's button says "Continue" except the final one, which says "Send my
  check-in". The test uses that to know when it's on the last step: it submits
  there and STOPS, then waits for the "Thank you" screen. No more extra lap.

DEPLOY
  1. GitHub Desktop: drop e2e/smoke.spec.ts into the repo (same path) -> commit -> push.
     No app files, no migration, no Vercel rebuild needed for this change.
  2. Re-run the E2E smoke (Actions -> E2E smoke -> Run workflow).

EXPECTED RESULT
  - With a pending check-in for test1@example.com: 4 passed.
  - If that prompt was already used up by an earlier run: 3 passed, 1 skipped
    ("No pending check-in to complete ..."). Seed a fresh week to get 4 again.

NOTE
  I can't run a real browser here; the live result comes from your workflow run.
  The spec type-checks against @playwright/test.
