BUILD: simplify-cockpit-64 — therapist surface slice 2b-i: two-pane cockpit shell.
DEPLOY THIS ZIP. DISCARD 21-63 and earlier.

MIGRATION: none new. If you have NOT yet run 0095_therapist_note (cockpit-63), run it —
the note channel needs it. Otherwise nothing to run for this build.

WHAT CHANGED (cockpit-64)
- The therapist patient page is now a two-pane cockpit on a wide desktop: a sticky
  left rail holds the context you read while you work (cycle/week, the clinic note,
  the patient's recent comments); the right column holds the actions, report-progress
  button, the note-to-clinic card, and the goals.
- On a phone (or compact layout) it stays a single column, exactly as before.
- Layout only — no behaviour changed. Reporting still happens via the "Report progress"
  button for now; the next slice folds rating into the goal cards.

DEPLOY
1. Unzip, push to GitHub. Vercel auto-builds. (Run 0095 first if you haven't.)
2. STILL OUTSTANDING (separate, cockpit-57): deploy the send-checkin-notifications
   Edge Function via the Supabase Dashboard.

QA (cannot verify here — please check on device)
[ ] On a wide desktop with the wide layout on: context sits in a left rail and stays put
    while the right column scrolls.
[ ] On a phone: everything stacks in one column as before (no rail).
[ ] The clinic note and patient comments read cleanly in the rail.
[ ] Action row, report-progress, note card, and goals all still work.

VERIFIED IN BUILD: tsc clean · next build 62/62 · i18n parity 1542 keys · font-stub restored.
