BUILD: simplify-cockpit-71 — the 1080 therapist cockpit layout (approved v2 mockup) + a fix.
DEPLOY THIS ZIP. DISCARD 21-70 and earlier.

MIGRATION: none new. Run 0095_therapist_note if you haven't.

WHAT CHANGED (cockpit-71)
- Goals are now HORIZONTAL BANDS on wide screens: the trend chart fills the left half, the
  rating fills the right half, split by a hairline. One short row per goal instead of a tall
  stack. On a phone the two halves stack.
- The physician's clinic note and the patient's recent comments now sit SIDE BY SIDE across
  the TOP (a context row), not in a left rail. The left rail is gone — this also removes the
  big empty-gap problem you saw earlier.
- The page (and its header) widened from 720 to 1080 to give the bands room.
- GoalProgressView learned a borderless mode so the chart sits cleanly inside the band with
  no card-inside-a-card.

FIX (this was a real bug in cockpit-70 — deploy 71 over it)
- In cockpit-70 the "Suggest a treatment change" label, its question, and its placeholder
  pointed at message keys that didn't exist, so they would have shown as raw text like
  "suggestChange". Now wired to the correct keys; they read properly.

DEPLOY: unzip, push to GitHub, Vercel auto-builds. (Still outstanding, cockpit-57: deploy
the send-checkin-notifications Edge Function.)

QA (cannot verify here)
[ ] Wide screen: each goal is one horizontal band — chart left, rating right.
[ ] Clinic note + patient comments are a row across the top, not a side column.
[ ] No large empty gap anywhere; page is ~1080 wide and matches the header width.
[ ] Phone: band halves stack; context stacks; everything reachable.
[ ] The treatment-change flag shows real wording (not "needsAdjustment"/etc.).
[ ] DA copy reads naturally (first-pass).

VERIFIED IN BUILD: tsc clean · next build 62/62 · i18n en/da parity 1568 keys · font-stub restored.
