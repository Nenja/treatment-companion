BUILD: simplify-cockpit-89 — fix the clinician HISTORY page 400 (stale length_weeks column).
No migration.

DISCARD 21-88 (superseded). Keep this zip.

THE PROBLEM YOU HIT:
  GET /rest/v1/treatment_cycle?select=id,cycle_number,start_date,length_weeks,status,clinician_note
  -> 400, history page blank.

CAUSE:
  treatment_cycle.length_weeks (and review_date) were DROPPED in migration 0010 and never re-added.
  The history hook (lib/supabase/patientHistory.ts, from cockpit-81) still selected length_weeks, so
  PostgREST rejected the whole query. This breaks history on ANY current database — it is NOT a
  missing-migration problem.

FIX (this build, app only):
  Removed length_weeks from the history hook's select string, its row mapping, and the HistoryCycle
  type. It was selected and stored but never rendered (the "weeks to next" figure shown in history is
  computed from injection-session dates, not from length_weeks), so nothing is lost.

AFTER DEPLOYING THIS: history should load. If it STILL 400s, the only other non-core column in that
  select is clinician_note (added by migration 0065) — run 0065 if it's still outstanding.

SEPARATE — to actually SEE demo history DATA, the enrichment must be applied:
  run 0099 (needs 0097 first), THEN reseed. Verify:
    select p.email, pt.current_medication, pt.etiology,
           (select count(*) from physio_assessment a where a.patient_id = pt.id) as physio_rows
    from profile p join patient pt on pt.profile_id = p.id
    where p.email like 'test%@example.com' order by p.email;

KNOWN FOLLOW-UP (REDCap dictionary): v2 still maps cycle_length_weeks and cycle_review_date to the
  dropped treatment_cycle columns. Those two fields can't be populated by the app and should be
  removed or remapped before the export is wired (dictionary v3). Say the word and I'll do it.

VERIFIED (this end): next build 62/62; tsc --noEmit clean.
DEPLOY: unzip -> GitHub -> Vercel. No SQL for this build.
