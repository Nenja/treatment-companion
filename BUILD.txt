BUILD: simplify-cockpit-90 — consent-button label tweaks + REDCap dictionary v3. No migration.

DISCARD 21-89 (superseded). Keep this zip.

WHAT CHANGED (app):
  • Background card, research row: the Withdraw button is now just "Withdraw" (was "Withdraw
    research consent") — matches the educational row's button.
  • Background card, educational row: the grant button is now "Record consent for educational use"
    (was "Allow educational use") — matches "Record research consent".
  • en + da. No schema change.

WHAT CHANGED (REDCap dictionary -> v3, treatment_companion_datadictionary_v3.csv, 84 fields):
  • Removed cycle_length_weeks and cycle_review_date. Both mapped to treatment_cycle columns that
    were DROPPED in migration 0010, so the export could never populate them. treatment_cycle form
    is now: cycle_index, cycle_start_date, cycle_status, cycle_modality.
  • The repo copy (redcap/treatment_companion_datadictionary.csv) was refreshed to v3.

DB STATE — no new migration. Confirm the backlog with schema_audit.sql if unsure.

VERIFIED (this end): next build 62/62; tsc --noEmit clean; i18n parity en/da equal.
DEPLOY: unzip -> GitHub -> Vercel. No SQL for this build.
