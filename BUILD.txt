BUILD: simplify-cockpit-81 — clinician history redesigned into a per-cycle clinical record (newest-first cycle cards; per-goal GAS sparklines with peak/fade; latest patient/clinician/physio ratings; symptom line with side effects flagged; summary strip on top). No new migration.

DISCARD 21-80 (superseded). Keep this zip.

DB STATE — RUN THESE IN SUPABASE IF NOT DONE:
  • 0095_therapist_note.sql        (from cockpit-77/78 — still pending on your side)
  • 0096_patient_care_team_notes.sql (from cockpit-78 — still pending on your side)
  cockpit-81 adds NO new migration.

WHAT CHANGED (code only, no DB):
  • NEW lib/supabase/patientHistory.ts — usePatientHistory(patientId): one read assembling
    cycles (newest first), injection per cycle, each goal's weekly self-report trajectory
    (GAS points + peak/fade), the latest patient/clinician/physio rating, a symptom summary
    (pain/stiffness first→last + side-effect week count), and weeks-to-next-cycle.
  • NEW components/clinician/GoalSparkline.tsx — compact SVG GAS(-2..2) line, zero baseline
    dashed, peak point sage-deep, fade point amber-deep, themeable for dark mode.
  • REWROTE app/[locale]/clinician/history/page.tsx — per-cycle cards + summary strip,
    current cycle open by default, older cycles collapsed. Old five charts/tables removed
    from this page (their hooks/components remain in the repo, now unused).
  • i18n: ~30 new keys under clinician.history (en + da, first-pass Danish). Parity 1609/1609.

VERIFIED: next build 62/62 pages · tsc --noEmit clean · i18n parity OK.

CANNOT verify from here (QA on your side):
  • Dark-mode contrast of sparkline points + summary/outcome chips.
  • The page against real multi-cycle data (rendered layout, sparkline shape).
  • Physio rater column populates once physio ratings exist.

DEFERRED (noted, not built): enum labels (guidance/modality/spasm-frequency/daily-care);
diagnosis header (column not yet in schema — pending the diagnosis-capture field).

DEPLOY: unzip → push to GitHub → Vercel auto-builds. Hard-refresh Firefox (Ctrl+Shift+R).
