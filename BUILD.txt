BUILD: simplify-cockpit-17   (lighter goal-Edit copy — versioning kept, ceremony dropped)
==========================================================================
MIGRATION: none. DB stays at 0090. Nothing to run in Supabase.

WHY: the versioning UX felt heavy. The versioning DATA MODEL (0086) stays — it
keeps every historical rating bound to the exact calibration it was scored
under, which matters for the record and the study. What felt heavy was the Edit
drawer announcing it. So this is a copy/UX change only.

DONE
- Edit drawer no longer talks about versions:
  - intro: "Saving creates version N ... keeps version M as history" -> "Your
    changes apply from here on. Past check-ins keep the exact wording and scale
    they were recorded under, so nothing already scored changes meaning." (No
    more {version}/{next} interpolation; the call is now t('intro').)
  - "Save new version" -> "Save changes"; "New goal version saved." -> "Goal
    updated."; error + carry-forward note de-jargoned.
- Nothing else surfaced versioning to the clinician: the v{version} badge isn't
  rendered anywhere, and the goal-history / link-goal modals aren't opened from
  the cockpit (removed in batch 5). Left in the tree, harmless.
- en + da both updated; i18n parity 0 mismatches.

VERIFIED LOCALLY
- tsc --noEmit clean.
- Production build OK via the font-stub procedure. 60/60 pages.

DEPLOY: no migration; unzip / push / Vercel builds. Page count 60.

** QA **
- Edit a goal: the drawer reads like a normal edit; button says "Save changes";
  success says "Goal updated." No "version" wording is visible to the clinician.
  (Underneath, a frozen version is still created — past ratings stay intact.)
