BUILD: simplify-cockpit-86 — DEV SEED: demo patients now populate the clinician HISTORY page.
Migration 0099 (DEV SEED ONLY — touches only test*@example.com). No app/build change.

DISCARD 21-85 (superseded). Keep this zip.

DB STATE — RUN IN SUPABASE (in order; 0099 is new this build):
  • 0090, 0095, 0096, 0097, 0098   (run any still pending — schema_audit.sql shows which)
  • 0099_dev_seed_history_extras.sql  (NEW — DEPENDS ON 0097 for the 'anoxic' etiology value)

THEN: trigger a reseed so the new data is applied —
  open the dev Scenarios page and use its reseed, or call dev_reseed_all() (service role).
  dev_reseed_all() now runs b1..b8 THEN dev_seed_history_extras().

WHAT 0099 ADDS (to test1..test6, so the history page can be checked):
  • patient.etiology + etiology_detail  -> the DIAGNOSIS pill (varies per patient; test6 = anoxic)
  • patient.current_medication / previous_medication -> the medication line
  • weekly_goal_rating.clinic_video_rating / _nrs   -> the CLINICIAN rating on each goal's trend
  • physio_assessment (per cycle) + physio_goal_rating (per goal) -> the PHYSIO rating column
  • a side effect on one mid-cycle check-in -> the amber side-effect flag
  • treatment_handoff note per cycle -> the per-cycle note shown in history

  Best scenario to inspect: "Longitudinal history — clinician view" (test6: 3 completed + 1 active).

VERIFIED (Method-D, local Postgres): all six field groups populate; re-running the function does
  NOT duplicate (base seed cascades cycle deletes; the function also clears its own rows first).

SEPARATE (not in this build) — REDCap data-dictionary completeness review (see chat):
  • Fix: `sex` is stored by the app (patient.sex) but the dict marks it study-team-entered —
      should auto-source (female=1, male=2, other/preferNotToSay=3).
  • Fix: `diagnosis` field-note says "app does not store this" but the annotation correctly
      sources patient.etiology — stale note, clean it up.
  • Decide: goal baseline NRS, clinician video ratings, "other" side-effect text, face-module
      fields (includes_face / pos_x), per-cycle & per-muscle notes — captured but not exported.
  Tell me which to fold in and I'll update the dictionary.

DEPLOY: unzip -> GitHub -> Vercel (no app change, so nothing to rebuild). Run 0099 + reseed in Supabase.
