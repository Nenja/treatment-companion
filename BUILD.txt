BUILD: simplify-cockpit-97 — ops runbook (Sentry tagging + OPS.md). CHANGED FILES ONLY.
No database migration. No SQL. No dashboard action needed right now.

>>> DEPLOY: drop the changed files into your repo (GitHub Desktop), commit, push. <<<
>>> DO NOT replace package.json or package-lock.json from the zip (they are older   <<<
>>> than your repo — same reason as cockpit-96).                                     <<<

WHAT THIS ADDS:
  - OPS.md (new): the operations runbook — how to run the pilot. Sentry alerts, Supabase
    backups/restore, deploy rollback, incident checklist, routine checks. It now opens with
    a §0 GO-LIVE CHECKLIST: the things to switch on the day you start using REAL patient data
    (Supabase Pro + backups, test restore, Sentry DSN, enforce CSP, Next.js update, compliance,
    Danish review). Backups are explicitly marked as deferred until that day - correct for now,
    since you're on test data.
  - lib/sentry.shared.ts: error reports now carry an environment + release tag so you can tell
    production errors from preview noise. This is DORMANT until you set a Sentry DSN at go-live -
    it changes nothing today. (The patient-data scrubbing was already in place; unchanged.)

ALSO BUNDLED FOR SAFETY:
  - lib/pwa.ts (the cockpit-96 build fix) is included again, so this deploys to a GREEN build
    whether or not you already applied cockpit-96. If you did, re-dropping it changes nothing.

CHANGED FILES:
  NEW:       OPS.md
  MODIFIED:  lib/sentry.shared.ts, lib/pwa.ts, DEPLOY.md
  DOCS:      HANDOVER.md, BUILD.txt, TRANSFER_PROMPT.md
  (No deletions or renames. No package.json/lock changes - do not copy those in.)

VERIFIED (this end): build 62/62; type-check clean; no schema or text changes.

NEXT: scope the mobile App Store / Play Store path (a planning step). Separately, upgrading off
next@15.1.9 (security advisory) is recommended as its own small change.
