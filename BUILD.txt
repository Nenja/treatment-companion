BUILD: simplify-cockpit-76 — therapist goals as a collapsed, click-to-rate list.
DEPLOY THIS ZIP. DISCARD 21-75 and earlier.

MIGRATION: none new. Run 0095_therapist_note if you haven't.

WHAT CHANGED (cockpit-76)
- Goals are now a list of closed rows. Tap a goal to open it; that reveals its trend chart
  and the rating. Opening a goal is the deliberate "I assessed this one" step — the goals you
  don't work on stay closed and out of the way, and the page stays short even with many goals.
- Once you rate a goal, its row shows a "✓ value" chip (e.g. ✓ 7, ✓ +1) so you can see what
  you've done without opening it. If you only suggested a treatment change, it shows "Flagged".
- The chart no longer repeats the goal's name (the row already shows it).

DEPLOY: unzip, push to GitHub, Vercel auto-builds. (Still outstanding, cockpit-57: deploy the
send-checkin-notifications Edge Function. Confirm Vercel rebuilds + hard-refresh Firefox.)

QA (cannot verify here)
[ ] Goals appear as closed rows; tapping one opens its chart + rating.
[ ] Rating a goal puts a "✓ value" chip on its row; "Flagged" if only a change was suggested.
[ ] The chart inside a row doesn't repeat the goal name.
[ ] Save still counts the goals you rated/flagged; submitting resets and re-collapses everything.
[ ] Works for both 0-10 (NRS) and GAS goals.

VERIFIED IN BUILD: tsc clean · next build 62/62 · i18n en/da parity 1570 keys · font-stub restored.
