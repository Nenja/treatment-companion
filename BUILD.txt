BUILD: simplify-cockpit-79 — patient can now READ the care-team notes (slice 2 of 2).
DEPLOY THIS ZIP. DISCARD 21-78 and earlier.

*** Requires migration 0096 (from cockpit-78). If you haven't run it yet, run
    0096_patient_care_team_notes.sql in the Supabase SQL editor first. ***

WHAT'S NEW
- The patient home gains a quiet "Notes from your care team" section, low on the page,
  collapsed by default and hidden entirely when there are no notes. Open it and the patient
  sees, verbatim and read-only:
    * the physician's per-visit handoff note (with a "Treatment adjusted" tag when flagged),
    * the physician's per-goal notes (tagged with the goal),
    * the physiotherapist's notes to the clinic.
  Physician notes carry a sage left edge, the therapist's an amber one. The patient cannot
  reply to or edit any of them.
- The clinician wording is corrected to match: the note-to-clinic box, the per-goal note hint,
  and the per-visit handoff hint no longer say "the patient never sees this" — they now say the
  patient can read it too. (Both English and Danish.)

WHAT TO CHECK ON THE LIVE SITE (can't be verified here)
- Run migration 0096 first, then with a patient that has at least one note, confirm the section
  appears and the notes read correctly. With no notes, the section should be absent.
- Dark-mode contrast of the section (left edges, chips, the count pill).
- Danish strings are a first pass — worth a native read of the new section + flipped hints.

DEPLOY: unzip + push to GitHub (Vercel auto-builds). Still outstanding: cockpit-57
send-checkin-notifications Edge Function; confirm Vercel rebuilds + hard-refresh (Ctrl+Shift+R).
