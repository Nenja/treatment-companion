BUILD — localization-sv-nb-12   (CUMULATIVE)  ***SV + NB TRANSLATION COMPLETE***
Swedish + Norwegian (Bokmal): pass 12 — the last professional texts.

>>> THIS ZIP IS CUMULATIVE <<<
Since you can't commit right now, this bundle carries EVERY file changed
across the recent passes. Deploying just this one zip applies everything.
Re-copying files you already committed does no harm.

INCLUDES ONE DATABASE MIGRATION
- 0103_allow_sv_nb_locales.sql — lets the database accept "sv"/"nb".
  SAFE TO RUN AGAIN if you already ran it.

THE BIG NEWS
The WHOLE APP is now translated into Swedish and Norwegian — 1639 of
1639 text strings each, the same coverage as Danish. That covers the
patient app, both therapist surfaces, the full physician console,
the admin console, the EHR export text, and every dialog and help text.

This pass added the last pieces: the professional "how it works"
walkthroughs and the page-help texts for clinicians and therapists.

IMPORTANT — NEEDS A NATIVE-SPEAKER CHECK
All Swedish and Norwegian text is my first pass. Before you use it with
real patients, please have a native Swedish speaker and a native
Norwegian speaker read through it. (Clinical terms like NRS, GAS, EMG
and units were left in their standard forms on purpose.)

WHAT IS NOT DONE YET
- The in-app LANGUAGE PICKER is not built yet. Right now the language
  follows the web address (/ = English, /da, /sv, /nb) and the saved
  profile setting. Next step is a simple picker on the login screen and
  in settings so people can choose between all four languages. The
  database and login code already accept all four.
- Push-notification text (the reminder messages) is still English/Danish
  only — a separate small job for later.

FILES IN THIS BUNDLE (keep folder structure)
  messages/en.json
  messages/da.json
  app/[locale]/goals/page.tsx
  i18n/routing.ts
  i18n/request.ts
  lib/supabase/auth.tsx
  supabase/migrations/0103_allow_sv_nb_locales.sql
  messages/sv.json
  messages/nb.json
  + HANDOVER.md, BUILD.txt, TRANSFER_PROMPT.md

HOW TO DEPLOY (when you can)
1. Supabase -> SQL editor -> run 0103_allow_sv_nb_locales.sql (safe to re-run).
2. Copy all files above into your repo, keeping folders.
3. GitHub Desktop -> Commit -> Push. Vercel rebuilds.

DO NOT: run "npm install", or overwrite package.json / package-lock.json.

WHAT'S NEXT
Build the in-app language picker (login + settings) for all four
languages — then optionally localize the push messages.
