=== 2026-06-28 — Questionnaire fill: add an exit ("Finish later") ===
- FIX: patients were trapped in the post-check-in questionnaire(s) — submit was the only control, no way out. Added an exit affordance to QuestionnaireForm (header "Finish later", shown when an onExit is provided). Because the core check-in is already saved at the seam before questionnaires render, exiting is non-destructive to the check-in; only the current questionnaire's in-progress answers are dropped.
- GUARD: if the current questionnaire has any entered answers, "Finish later" opens an inline confirm (leaveBody + Leave / Keep going) so taps don't lose typed answers; with nothing entered it exits immediately.
- WIRING: PostCheckinQuestionnaires passes onExit={() => setIndex(dueList.length)}, which skips the remaining questionnaires and lands on the existing thank-you (which confirms the check-in saved + offers home). Already-submitted questionnaires in the sequence remain saved.
- i18n: questionnaireFill.finishLater / leaveBody / leaveConfirm / keepGoing (en + da; da first-pass). No schema change.
- VERIFIED: tsc 0; eslint 0; i18n parity; font-stub build 105/105; layout SHA restored.
- NOTE: this is an EXIT, not a per-question Back. Going back to re-edit an already-submitted questionnaire in a multi-questionnaire sequence isn't supported (each submits on continue). Say the word if you want step-back between questionnaires.
=== 2026-06-28 — Full questionnaire-response history restored, in the History tab ===
- Kept the new "Since last visit" buttons (recent, grouped by questionnaire) AND re-added the full responses archive — but now on the clinician History page (/clinician/history), not the QuestionnairePanel. New QuestionnaireResponsesSection at the bottom of the cycle-by-cycle list: ALL responses for the patient (all cycles), newest first, each expandable to its prompt/answer pairs. Read-only, no score.
- DEDUP: extracted formatAnswer into lib/questionnaireAnswers.ts; VisitChanges and the History section both import it (removed VisitChanges' local copy + its now-unused QuestionnaireResponseItem import).
- i18n: reuses clinician.questionnaires.responsesHeading/responsesLoading/responsesError/responsesNone (which had gone unused when the panel section was removed — now used again) + respWeek/respNoItems/filled*/answer*. NO new keys; parity intact.
- History page reads usePatientQuestionnaireResponses(session patientId) — same access-gated 0119 RPC.
- VERIFIED: tsc 0; eslint 0; i18n parity; font-stub build 105/105; layout SHA restored.
- CANNOT VERIFY (deployed): the History section rendering with real responses; that it shows ALL cycles (vs the "Since last visit" recent-only view).
=== 2026-06-28 — Questionnaire responses moved to "Since last visit" (one button per questionnaire) ===
- Responses no longer live in the QuestionnairePanel (removed ResponsesSection + formatAnswer + unused imports there). They now surface in the "Since last visit" card (components/clinician/VisitChanges.tsx): responses since the anchor (last treatment, else cycle start) are grouped by questionnaire; each group renders a button (title + count badge) alongside the existing Videos buttons. Clicking opens a modal (useModalA11y) listing each response newest-first with date · week · filled-by and the per-item prompt/answer pairs (formatAnswer maps option values->labels, multi->labels, boolean->Yes/No). Read-only, no score.
- Data via usePatientQuestionnaireResponses(patientId) (already gated to patient/clinician/admin by 0119). Reuses clinician.questionnaires answer/filled/respWeek/respNoItems keys; clinician.questionnaires.responsesHeading/Loading/Error/None are now unused (kept; harmless, parity intact). New key visitChanges.questionnairesHeading (en + da).
- SCOPE NOTE: the buttons show questionnaires answered SINCE the anchor (consistent with the card). Responses are normally check-in-linked so they appear when there are check-ins since the visit; a clinician/caregiver-submitted response with no check-in would also surface (block renders independently of the check-in list).
- VERIFIED: tsc 0; eslint 0; i18n parity; font-stub build 105/105; layout SHA restored.
- CANNOT VERIFY (deployed): the button row + modal rendering with real responses; that older (pre-anchor) responses are intentionally not shown here (full history is still available via export / RPC; say the word for an all-history view).
=== 2026-06-28 — Library moved behind an "Available questionnaires" toggle ===
- The "Add from the library" block (search + language filter + list) crowded the QuestionnairePanel and would dominate it with a dense library. It's now collapsed behind an "Available questionnaires" toggle button (libraryToggle); opening it reveals the search/filter/list. useLibraryQuestionnaires gained an `enabled` arg (default true) and is passed libraryOpen, so the library is only FETCHED when opened (lazy). Enabled list + Responses remain always-visible above it.
- New key clinician.questionnaires.libraryToggle (en + da; da first-pass). libraryHeading retained (still used as the search/section context, now unused header text — harmless, parity intact).
- VERIFIED: tsc 0; eslint 0; i18n parity; font-stub build 105/105; layout SHA restored.
=== 2026-06-28 — Library search (clinician QuestionnairePanel) ===
- Added a search box to the "Add from the library" list. Free-text, client-side, case-insensitive; matches questionnaire title, description, or key. New librarySearch state; libraryRows derived by filter. Empty states distinguished: nothing published -> libraryEmpty; published but filtered out -> searchNoMatches. Sits under the language filter; works together with it. New keys clinician.questionnaires.searchPlaceholder / searchNoMatches (en + da; da first-pass).
- VERIFIED: tsc 0; eslint 0; i18n parity en==da; font-stub build 105/105; layout SHA restored.
=== 2026-06-28 — Library items collapsed by default (clinician QuestionnairePanel) ===
- The "Add from the library" list rendered each questionnaire as a tall card (title + description + item count + cadence select + Enable). With several published it was a long wall. Each item is now a collapsed row: title + (item count · LANG · "Enabled" if already on) + chevron; tap to expand the description, cadence picker and Enable button. Per-key open state (libOpen), collapsed by default; aria-expanded set. No i18n changes (reused existing keys); no schema change.
- VERIFIED: tsc 0; eslint 0; font-stub build 105/105; layout SHA restored.
- NOTE: this is the clinician per-patient library ("Add from the library"). The admin page's "Questionnaires" list (existingHeading) is already compact single rows.
=== 2026-06-28 — Questionnaires folded into the check-in flow + clinician responses view ===
- UX FIX (the "why fill it AFTER finishing?" problem): the check-in wizard showed its own "step N of N" and a Submit button, then ambushed the patient with questionnaires. Now they're ONE continuous flow with a single finish; core check-in is still saved first (data integrity), but the patient is never told they're done until they actually are.
- NEW MIGRATION 0119 (read-only, no schema change), validated in PG16 harness:
    * due_questionnaires_for_week(patient, week) — same resolution as due_questionnaires_for_checkin but keyed by (patient, week), so the wizard can learn what's due BEFORE submit and count it into the total. Patient/clinician access-gated.
    * list_patient_questionnaire_responses(patient) -> jsonb — newest-first responses with per-item answers (incl. options for value->label), for in-app display. Gated to the patient themselves (data ownership), a clinician with an active session, or admin. Raw only, no scoring. Harness verified: due-by-week returns the assignment; responses return full record (week, items, values); patient B reading patient A -> 'no access to this patient'.
- CHECK-IN WIZARD (app/[locale]/checkin/page.tsx): resolves dueQ = useDueQuestionnairesForWeek(patientId, prompt.weekNumber) up front. WizardLayout totalSteps now = coreSteps + dueCount (unified counter via hideStepBar's "Step X of Y"). Last core step: button reads 'Continue' (continueToQuestions) when questionnaires follow, else 'Submit'; disabled while dueQ.isLoading so the label is never a false finish. Internal step logic (isLastStep etc.) still uses coreSteps — unchanged.
- POST-CHECK-IN (components/patient/PostCheckinQuestionnaires.tsx): now a PURE stepper over a pre-resolved dueList passed from the wizard (no post-submit refetch -> no thank-you flash). Takes stepOffset + displayTotal so each questionnaire shows "Step 6 of 7" continuing the wizard count. Thanks renders only after the last questionnaire (or immediately if none).
- QuestionnaireForm.tsx: button now reads 'Continue' mid-sequence and 'Finish' on the last (questionnaireFill.continueLabel/finish), reinforcing one flow.
- CLINICIAN RESPONSES VIEW (components/clinician/QuestionnairePanel.tsx): new Responses section between Enabled and Library. usePatientQuestionnaireResponses(patientId) lists each submission (title · date · week · filled-by), expandable to prompt/answer pairs. formatAnswer() maps option values->labels, multi_choice JSON->labels, boolean->Yes/No, nrs/number->value, text->text. New lib hooks + types in lib/supabase/questionnaires.ts.
- i18n (en + da; da FIRST-PASS, flag for native review): patient.checkin.continueToQuestions; questionnaireFill.continueLabel/finish; clinician.questionnaires.{responsesHeading,responsesLoading,responsesError,responsesNone,respWeek,respNoItems,filledSelf/Caregiver/Clinician/Therapist,answerYes,answerNo}.
- VERIFIED: tsc 0; eslint 0 errors (pre-existing tA11y/a11y warnings only); i18n parity en==da; font-stub build 105/105; layout SHA restored 939245...
- USER DB ACTION: run 0119_questionnaire_due_by_week_and_responses_read.sql in Supabase (after 0114-0116). Standalone at outputs/.
- CANNOT VERIFY (QA on deployed app): the unified counter end-to-end with a real assignment (assign sample_symptoms every_checkin to test1, do a check-in, confirm "Step X of Y" runs continuously into the questions and a single Thanks at the end); the clinician Responses list populating after a submit; da strings.
=== 2026-06-28 — Admin page: patient record ID surfaced + all sections collapsed + reorder ===
- WHY: the questionnaire assignment (and similar) target the patient RECORD id (patient.id), but the admin accounts list is built from PROFILES and AdminAccount.id is the profile/account id — the right id was nowhere in the UI, forcing a SQL lookup.
- patient.id now reaches the panel: app/api/admin/list-accounts/route.ts selects 'patient(id)' (PostgREST reverse embed; patient.profile_id->profile.id is unique so to-one) and returns patientId (object|array handled). lib/supabase/admin.ts AdminAccount gains patientId: string|null.
- AccountRow (expanded detail) now shows a copyable "Patient record ID" row for patient-role accounts (mono + Copy button -> clipboard + toast). New keys admin.colPatientId / patientIdCopied / copyId (en + da; da is FIRST-PASS, flag for native review).
- COLLAPSE-ALL: every top-level section now defaultOpen={false} (studies, questionnaires, export, purge added; accounts/create already false). The Access panel was a bespoke h2 + Show/Hide block — folded into the same Collapsible card: Collapsible gained an onToggle(open) hook, AccessSection is now embedded + driven by a parent accessOpen state (preserves lazy-load: the active-access list only fetches once its section is expanded).
- ORDER REVIEW: research group reordered to a setup→operations→lifecycle flow: Studies, Questionnaires, Export, Purge (was Export, Studies, Purge, Questionnaires). Questionnaires was also MISSING from the side-nav — now added, in the new order. Accounts group unchanged (Accounts, Create, Access).
- VERIFIED: tsc 0; eslint 0 errors (pre-existing a11y/hooks warnings only); i18n parity (en/da equal, _meta aside); font-stub build OK 105/105 pages; layout.tsx SHA restored 939245...
- CANNOT VERIFY (QA on the deployed app):
    * That PostgREST resolves the profile->patient embed and the Patient record ID actually renders — expand a patient account and confirm. patient has exactly one FK to profile (profile_id), so 'patient(id)' should be unambiguous; if it ever errors, disambiguate as 'patient!patient_profile_id_fkey(id)'.
    * Clipboard copy + toast on the device/browser.
    * Visual: every section collapsed on load; Access still lazy-loads its list only when expanded; nav jump opens + scrolls each section (goTo opens <details>).
=== 2026-06-26 — Login: surface the real auth error (test sign-in debugging) ===
- The login banner only ever showed friendlyError's fallback ('Something went wrong signing in'), hiding the actual Supabase message. signInWithPassword IS erroring (not a credential error, or it'd say so) — root cause is server/config-side, invisible from the repo.
- app/[locale]/login/page.tsx: capture code + HTTP status + raw message; console.error them; show a small mono detail line under the banner for any NON-credential failure (a plain wrong password stays clean). Added friendlyError status arg + errServer branch (500 / 'database error' / 'unexpected') with a 'this isn't your password' message (login.errServer, en+da).
- NOTE: profile fetch selects design_variant (needs 0117). If 0117 isn't applied in an env, sign-in SUCCEEDS but profile is null -> stuck on login with NO banner (different symptom). Not the cause of the screenshot, but verify 0117 is applied.
- VERIFIED: tsc 0; eslint 0; build OK; i18n parity; layout SHA restored. Pre-pilot: consider gating the detail line off before real patients use it.

=== 2026-06-26 — Migration 0118: patient owns the data (notes no longer hidden) ===
- PRINCIPLE FIX: the patient owns their record; nothing about them is hidden from them. Three tables were built 'RLS-isolated from the patient' (clinician-only SELECT, no patient policy):
    * treatment_handoff (0088)  — physician->therapist cycle handoff note
    * goal_handoff_note (0090)  — physician->therapist per-goal handoff note
    * archived_goal_video (0092) — patient's own archived clips + the clinic score they carried
  0118 adds a patient self-read (patient_id = current_patient_id(), the canonical schema-wide predicate) to all three. READ ONLY — authoring is unchanged (still physician/clinician via the existing SECURITY DEFINER RPCs).
- TESTED (PG16 harness): policies created; patient A reads own rows in all three; a different patient reads zero (RLS scopes correctly).
- Tour 'Note to the therapist' copy now tells the clinician the note is patient-readable (write it for the patient too) — corrects my earlier wrong 'patient doesn't see it'.
- DELIBERATELY LEFT (flagged): questionnaire / questionnaire_library / study are clinician-authored DEFINITIONS, not info about a patient; visit_code_unlock_attempt is a security audit log. Say the word to extend the principle to any of these.
- FOLLOW-UP (not built): these notes are now ACCESSIBLE to the patient via RLS, but there is no patient-facing SCREEN that shows them yet. Surfacing them in the patient UI is a separate build if you want them visible in-app (vs. accessible via export / subject-access).
- USER DB ACTION: run 0118_patient_owns_notes.sql in Supabase SQL editor (after 0115/0116/0117). Standalone at outputs/0118_patient_owns_notes.sql.
- VERIFIED: build OK; i18n parity; layout SHA restored.

=== 2026-06-26 — Tour rethink (3 roles) + tutorial-replay move + notes-copy fix ===
- Reframed every tour as an essentials-first walkthrough with brief mentions of extras.
- Cockpit (clinicianPatient) reordered to an intuitive flow: patient -> overview -> goals -> treatment -> actions -> end. Goals step is now the centrepiece (graphs + video review + retirement); 'More tools' (suggestions / therapist input / history / export) demoted to a short mention.
- patientHome check-in step + physioPatient rate step reworded to name the ONE core weekly action explicitly.
- Treatment 'Note to the therapist' copy CORRECTED: removed the 'patient doesn't see it' claim (now neutral). See note below re: actual RLS.
- Moved 'Show tutorial again' from the account menu to Profile > Help (new profile.sectionHelp / tutorialReplayHelper keys). Routed through attemptLeave so unsaved profile edits are confirmed first; hard-navs to role home where the wizard mounts.
- VERIFIED: tsc 0; eslint 0 errors; build OK; i18n parity; layout SHA restored.
- OPEN DECISION (needs Nikolaj): handoff note patient-visibility. migration 0088 has a HARD RULE 'NEVER patient-visible' (separate treatment_handoff table + RLS); goal_handoff_note (0090) likewise. Making patients able to read clinician<->therapist notes is a deliberate RLS/data-model reversal with privacy/DPIA implications — NOT changed in code; awaiting confirmation.

=== 2026-06-26 — Tour copy + coverage fixes from live QA ===
- Cockpit (clinicianPatient): split the overview step — 'Record a treatment' is now its OWN step on the new-treatment button (data-tour=treatment). Goals step now mentions per-goal VIDEO review and goal RETIREMENT. Overview narrowed to context + since-last-visit note.
- Treatment page: tour expanded 3->6 steps — intro, modules (body/face toggle, data-tour=modules), copy-from-last-treatment (data-tour=copylast), setup (#tsec-setup), muscles (#tsec-muscles), and Note-to-therapist (#tsec-handoff). Covers the three things that were missing (therapist note, copy-last, module choice).
- Rewrote ALL tour step copy (clinicianPatient, treatment, goals, physioPatient, patientHome, history, suggestion) to be concrete and consistent — says what each thing is AND what you do there. en+da, parity preserved.
- VERIFIED: tsc 0; eslint 0 errors; build OK; layout SHA 939245bb...66fc restored.
- NOT Claude-verifiable: live spotlight positioning of the new anchors (treatment modules/copylast/handoff; cockpit treatment button) and that the face/handoff sections are present when expected — QA pass.

=== 2026-06-26 — Tour rollout: clinician treatment, history, suggestion ===
- Registry += treatment (intro, #tsec-setup, #tsec-muscles — reuses existing section ids), history (summary, cycle-by-cycle), suggestion (patient's suggestion, your decision). data-tour anchors added; '?' on each now launches the tour + one-time nudge via the registry.
- new-goal left on text help by design: an 84-line wrapper around the shared RecordGoalForm; a spotlight there is low value and would push data-tour into a shared component.
- i18n: tour.{treatment,history,suggestion}.* (en+da). Parity preserved.
- VERIFIED: tsc 0; eslint 0 errors; build OK; layout SHA 939245bb...66fc restored.
- Tour now on: clinicianPatient, patientHome, goals, physioPatient, treatment, history, suggestion. Text help retained: checkin (wizard), new-goal (shared form), clinician landing (single action).

=== 2026-06-26 — Tour rollout: patient goals + physio patient ===
- Registry (lib/tourSteps.ts) += goals (2 steps: your goals/graphs, suggest a goal) and physioPatient (4 steps: patient, rate the visit, suggest a goal, end session). data-tour anchors added to app/[locale]/goals/page.tsx and app/[locale]/physio/patient/page.tsx. Both pages already had the '?', so they now launch the spotlight tour + get the one-time nudge automatically via the registry.
- Skipped the clinician LANDING (visit-code unlock) — single-action screen with no '?'; a spotlight tour adds nothing there.
- i18n: tour.goals.* + tour.physioPatient.* (en+da). Parity preserved.
- VERIFIED: tsc 0; eslint 0 errors; build OK; layout SHA 939245bb...66fc restored.
- Remaining easy candidates (anchors + registry entry + strings): clinician treatment, history, new-goal, suggestion.

=== 2026-06-26 — Tour rollout (registry) + patient-home tour + first-visit nudge ===
- Refactor to a pageKey-keyed tour registry (lib/tourSteps.ts): TOURS[pageKey] = ordered {target, key}. PageHelpButton resolves steps from the registry + tour.{pageKey}.{key}Title/Body, so any page becomes tourable with just data-tour anchors + a registry entry + strings — no layout-prop threading (works through AppShell/AppHeader/WizardLayout, which only pass pageKey). Reverted the clinician-patient inline tourSteps prop accordingly.
- NUDGE: one-time, dismissible 'New here? Take a quick tour' popover by the '?' on tour pages. Appears after an 800ms beat on first visit; 'Take the tour' launches it, 'Not now' dismisses; opening '?' also marks seen. Seen state in localStorage per pageKey (per-device, low-stakes hint — not account state; no migration). Falls through gracefully if storage is blocked.
- Patient HOME tour added (pageKey patientHome): 4 steps — welcome/cycle context, weekly check-in CTA, your goals, visit code — with data-tour anchors on app/[locale]/page.tsx.
- Note: check-in stays on contextual TEXT help by design — a fixed spotlight fits a single dense screen, not a multi-step wizard whose targets change between steps.
- i18n: tour.{clinicianPatient,patientHome}.* (nested), help.tourNudge/Start/Dismiss, en+da; old flat clinician.patient.tour* removed. Parity preserved.
- VERIFIED: tsc 0; eslint 0 errors; build OK; layout SHA 939245bb...66fc restored.
- NOT Claude-verifiable: spotlight positioning + nudge placement on live pages (esp. home vs clinician layouts) — QA pass.

=== 2026-06-26 — Guided spotlight tour on '?' (clinician patient page) ===
- New components/feedback/GuidedTour.tsx: reusable spotlight/coach-mark engine. Dims screen, box-shadow spotlight over one target at a time, caption card (Back/Next/Done + step counter), keyboard (arrows/Enter/Esc), focus to card, prefers-reduced-motion aware, theme-token styled. resolveTarget() picks the first VISIBLE match so layout variants (toolbar/sidebar/body) resolve correctly.
- PageHelpButton: optional tourSteps prop. When provided, '?' launches the tour directly; otherwise falls back to the existing text help modal (other pages unchanged).
- clinician/patient/page.tsx: data-tour anchors (patient header, action row x3 variants, overview/since-last-visit, active goals, end-session) + 5-step tour passed to PageHelpButton.
- ALSO this delivery: therapist-visits strip replaced with actual visit-date chips (earlier in session).
- i18n: tour.{back,next,done,skip,step} + clinician.patient.tour* (5 steps), en+da. Parity preserved.
- VERIFIED: tsc 0; eslint 0 errors; build OK; layout SHA 939245bb...66fc restored.
- NOT Claude-verifiable: spotlight POSITIONING and scroll behaviour on the live page — QA pass needed (resize, sidebar vs top nav, narrow). Easy to tune (PAD / card placement in GuidedTour).

=== 2026-06-26 — Fix: therapist-visits display (treatment page) ===
- TherapistInputPanel showed a Mon-Sun weekday strip that collapsed every visit in the (multi-week) cycle onto one 7-day row (each visit reduced to its weekday). Count was correct; the strip was misleading ('looks like one week').
- Replaced with chips of the ACTUAL visit dates (locale-formatted weekday+day+month via useLocale + Intl). Removed DAY_SHORT_KEYS / therapyWeekdaysIso / tTraining / physioWeekdaysAria usage. tsc 0, eslint 0, build OK, layout SHA restored.

=== 2026-06-26 — Editorial is now the default + design choice in onboarding ===
- Default flipped to EDITORIAL: globals.css :root now holds the near-square radii; new [data-design='current'] rule restores the rounded look for users who pick it. Baseline approach = no rounded->square flash on load, and editorial applies even before JS.
- lib/design.ts: DEFAULT_DESIGN='editorial'; resolveDesignId now maps explicit 'current'->current, 'editorial'->editorial, NULL/unknown->editorial. Existing profiles (design_variant NULL) render editorial automatically; an explicit 'current' choice is preserved.
- Onboarding (intro) Comfort step: new ComfortDesign segmented control (Rounded | Editorial) beside text-size/brightness, persists via useSetDesign — so the choice is offered at first-run setup, not only in profile. Editorial shows selected by default.
- i18n: intro.comfortDesign/comfortDesignRounded/comfortDesignEditorial (en+da). Parity preserved.
- No new migration (0117 column already covers it; default is resolved client-side + CSS).
- VERIFIED: tsc 0; eslint 0 errors; production build OK; layout SHA 939245bb...66fc restored.
- NOT Claude-verifiable: live appearance of editorial-by-default across screens + onboarding round-trip — QA in the running app (needs 0117 applied).

=== 2026-06-26 — Design-direction picker + trimmed palettes ===
- Migration 0117: profile.design_variant text (NULL=current). No new RLS (row-level self-update already covers it). Harness: applies clean, column present.
- New axis orthogonal to colour: lib/design.ts (DesignId current|editorial, resolveDesignId, applyDesign sets data-design on <html>). globals.css: [data-design='editorial'] overrides --radius-card/--radius-button to near-square. Headings already serif + eyebrows already uppercase, so radius is the defining flip.
- useSetDesign() in colorScheme.ts (mirrors useSetPalette: optimistic apply + patchProfile + persist design_variant). ThemeApplier applies saved design on load. auth.tsx: profile gains designVariant (select + mapping; row cast until types regen).
- AppearanceSettings: new Design picker (Rounded | Editorial) with rounded-vs-square preview swatch, beside the palette grid.
- Palette TRIM: removed Plum and Slate. Kept Green (default), Clay (warm), High contrast (accessibility). PaletteId/PALETTE_IDS/PALETTES updated; resolvePaletteId maps removed/legacy ids -> green safely.
- i18n: appearance.designTitle/designHint/designCurrent/designEditorial (en+da). Parity preserved.
- VERIFIED: tsc 0; eslint 0 errors; production build OK (rc=0); layout SHA 939245bb...66fc restored.
- USER ACTIONS: run 0117 in Supabase; regenerate database.types after (drops the design_variant row/update casts). NOT Claude-verifiable: live radius flip + picker round-trip under real auth — QA in the running app.

=== 2026-06-25 — Questionnaire engine slice 5: language + REDCap export ===
- Migration 0116 (run AFTER 0115): +questionnaire.lang (default 'en'); create_questionnaire +p_lang (drop 7-arg -> 8-arg); list_library_questionnaires +lang return +optional p_lang filter; list_patient_questionnaires +lang; due_questionnaires_for_checkin +lang; NEW export_questionnaire_responses() (clinician-gated, RAW long-format, mirrors export_research_dataset gate). Existing export_research_dataset UNTOUCHED.
- HARNESS: 0114+0115+0116 apply clean; T9 (lang stored + library lang filter) + T10 (export returns raw items for consented patient, blocks non-clinician) PASSED; prior T1-T8 still pass.
- REDCap: buildRows +questionnaireRows() + COLUMNS q_* (generic long-format repeating instrument 'questionnaire_item', one row per item answer — fits arbitrary admin-authored questionnaires). redcapExport.ts + runSync.ts fetch export_questionnaire_responses and merge rows. redcap/treatment_companion_datadictionary.csv += questionnaire_item instrument (10 fields). vitest 12/12.
- Language UI: admin form language select (en/da/sv/nb) + lang badge in list; clinician panel language filter dropdown + lang badges. Questionnaire CONTENT is stored as authored, not app-localized.
- VERIFIED: tsc 0; eslint 0 errors; production build OK; layout.tsx SHA 939245bb...66fc restored.
- USER ACTIONS: run 0116 in Supabase (after 0115); re-import the updated data dictionary CSV into REDCap so the questionnaire_item instrument exists before syncing; (optional) regenerate database.types.

=== 2026-06-25 — Questionnaire engine slice 4: admin authoring + publish UI ===
- lib/supabase/questionnaires.ts: +useAdminQuestionnaires (lists latest version per key + published flag via admin RLS on questionnaire/questionnaire_library), +useCreateQuestionnaire (create_questionnaire), +useSetLibraryVisibility (set_library_visibility).
- NEW components/clinician/QuestionnaireAdminSection.tsx: existing-list with Publish/Unpublish toggles + a New-questionnaire form with a dynamic item builder (all 7 item types, options editor for choice/likert, min/max for number, required), licensed flag + source note, publish-now toggle.
- clinician/admin/page.tsx: import + a <Collapsible id=questionnaires> block beside the research sections (admin-gated, embedded).
- i18n: admin.questionnairesTitle + questionnaireAdmin.* (en + da first-pass); parity preserved.
- VERIFIED: tsc 0; eslint 0 errors (build-passing). Production build OK (105/105 pages); layout.tsx SHA 939245bb...66fc restored.
- a11y note: the admin section carries 8 jsx-a11y/control-has-associated-label warnings on controls that DO have visible <label> text (rule is finicky about span-nested labels), consistent with the existing admin sections. Non-blocking; admin-only screen. Minor polish QA item.
- Needs 0114+0115 applied + (optional) database.types regen. Rendering + live round-trips are admin QA.

=== 2026-06-25 — Questionnaire engine slice 3: patient-side renderer ===
- lib/supabase/questionnaires.ts: +useDueQuestionnaires (due_questionnaires_for_checkin), +useQuestionnaireItems (questionnaire_item select via narrow cast), +useSubmitQuestionnaireResponse (submit_questionnaire_response).
- NEW components/patient/QuestionnaireForm.tsx: dynamic renderer for all item types (nrs_0_10, number, text, boolean, single_choice/likert, multi_choice); required validation; raw-answer encoding; a11y labels.
- NEW components/patient/PostCheckinQuestionnaires.tsx: resolves due questionnaires for the just-submitted check-in and steps through them, then falls through to ThanksView. Wizard untouched.
- checkin/page.tsx: wrapped the post-submit ThanksView in PostCheckinQuestionnaires(weeklyCheckinId=submittedId). Questionnaires appear AFTER a successful check-in (the only point the weekly_checkin id exists).
- i18n: questionnaireFill.* (en + da first-pass); parity preserved.
- VERIFIED: tsc 0; eslint 0 errors (new files clean); production build OK (105/105 pages); layout.tsx SHA 939245bb...66fc restored.
- Needs 0114+0115 applied + database.types regen (drops the rpc/from casts). Rendering + live round-trips are patient-app QA.

=== 2026-06-25 — Questionnaire engine slice 2: clinician 'Questionnaire' tool panel (UI) ===
- NEW lib/supabase/questionnaires.ts: react-query hooks (useLibraryQuestionnaires, usePatientQuestionnaires, useAssignQuestionnaire, useSetAssignmentActive) over the 0114/0115 RPCs.
- NEW components/clinician/QuestionnairePanel.tsx: CockpitPanelDrawer panel — pick from published library + choose cadence (every / every-2 / monthly / first-of-cycle / baseline) + manage enabled (stop/restart); study-level shown read-only.
- PatientActionRow.tsx: added 'questionnaires' action (type+items+icon).
- clinician/patient/page.tsx: import, actionLabels/actionShortLabels entries, showQuestionnaires state, onActionSelect branch, panel render (patientId={patient.id}).
- i18n: clinician.patient.action(Short)Questionnaires + clinician.questionnaires.* (en + da first-pass); key-set parity preserved (only benign _meta diff).
- VERIFIED: tsc 0 errors; eslint 0 errors (13 pre-existing warnings, none new); production build OK (clinician/patient compiled, 90 prerendered); layout.tsx restored to SHA 939245bb...66fc.
- TYPES NOTE: questionnaires.ts calls the new RPCs through a narrow rpc cast because lib/database.types.ts predates 0114/0115. After applying 0115 in Supabase, regenerate database.types and the casts can be dropped.
- NOT UI-VERIFIABLE BY CLAUDE: actual rendering, live RPC round-trips under real auth/RLS, and DA wording — QA in the running app. Requires 0114+0115 applied first.

=== 2026-06-25 — Questionnaire engine split into 0114 (foundation, AS APPLIED) + 0115 (library+clinician delta) ===
- REASON: 0114 was already applied; create_questionnaire changed 6->7 args, so re-running an edited 0114 would leave a stale overload. Forward-only fix instead.
- 0114 restored in repo to exactly what was applied (foundation; no library).
- 0115 = forward delta: questionnaire_library (+backfill publishes existing keys), drops old create_questionnaire(6-arg) and adds 7-arg w/ publish, clinician-capable assign_questionnaire, + set_library_visibility / list_library_questionnaires / list_patient_questionnaires / set_questionnaire_assignment_active.
- VERIFIED on PG16: apply 0114 then 0115 -> zero errors, exactly ONE create_questionnaire overload, ALL_TESTS_PASSED + T8_PASSED.
- ACTION: you already ran 0114 -> run 0115 ONLY (Supabase SQL editor). Do NOT re-run 0114.

=== 2026-06-25 — Questionnaire engine 0114 UPDATED: library + clinician-driven assignment ===
- Added questionnaire_library (key-level publish, admin-curated).
- create_questionnaire: +p_publish (default true) -> upserts library. Admin-only.
- New RPCs: set_library_visibility (admin publish/unpublish), list_library_questionnaires (care-pro/admin picker),
  list_patient_questionnaires (access-gated), set_questionnaire_assignment_active (stop/restart).
- assign_questionnaire now allows a CLINICIAN with active session to enable a PUBLISHED questionnaire for THAT patient + cadence; study-level stays admin-only; non-admins limited to library.
- Re-VERIFIED on PG16: ALL_TESTS_PASSED (T1-T7) + T8_PASSED (library/publish, clinician enable+cadence, study/unpublished/no-access blocks, non-admin publish block, toggle access control).
- Pure SQL; not yet applied -> review then run in Supabase SQL editor (after 0113).

=== 2026-06-25 — Questionnaire engine (slice 1: data foundation) ===
- migration 0114_questionnaire_engine.sql: questionnaire / questionnaire_item / questionnaire_assignment / questionnaire_response / questionnaire_item_response
- RAW-CAPTURE ONLY (no scoring stored); versioned defs; assign to study OR patient with schedule rule; responses freeze to version + link to weekly_checkin
- RPCs: create_questionnaire, assign_questionnaire, due_questionnaires_for_checkin, submit_questionnaire_response (+ _questionnaire_due_for_week helper)
- RLS mirrors weekly_checkin (patient self / clinician-access / admin). VERIFIED on throwaway PG16: admin gate, versioning, assignment constraints, schedule resolver (specific_weeks/every_checkin/every_n + latest-version), raw capture, required/NRS-range/cross-patient/foreign-checkin guards, care-pro submit, RLS isolation. ALL_TESTS_PASSED.
- No app-code change (pure SQL); tsc/lint/build unaffected. NOT YET APPLIED in Supabase — review then run in SQL editor.

=== 2026-06-24 — iOS go-live developer-ready artifacts ===
- mobile/assets/{icon.png(1024 opaque),splash.png,splash-dark.png(2732)} for @capacitor/assets
- mobile/IOS_GOLIVE.md (consolidated iOS handoff brief: accounts, push wiring, cloud build, TestFlight, privacy, 4.2 caveat)
- codemagic.example.yaml (iOS build+TestFlight template, root)
- mobile/README.md appId note corrected to dk.mprc.treatmentcompanion
- NO app-code change; web/app already iOS-ready. iOS native project NOT generated here (must be cap add ios + pod install on macOS/cloud).

=== 2026-06-23 — iOS / PWA install support ===
- Added PNG icons (iOS ignores SVG apple-touch-icon): public/apple-touch-icon.png (180, opaque), icon-192.png, icon-512.png, icon-maskable-512.png
- layout.tsx metadata.icons -> PNG (apple-touch-icon 180); manifest.json icons -> PNG; sw.js notification icon -> PNG
- Verified: stubbed prod build 'Compiled successfully'; tsc 0 errors; eslint 0 errors; i18n parity intact
- FONT-RESTORE SHA (new canonical app/[locale]/layout.tsx): 939245bbbfdde208ac38b61cf8769621a0eb08be316ba326bfe4e76eebae66fc
- (Supersedes prior layout.tsx restore SHA d6901997...; layout legitimately changed for the apple-touch PNG)

BUILD - v1.0.0   (stack-gap hardening folded into the pre-lock baseline · cumulative)

WHAT THIS IS
  The version baseline for testing (v1.0.0, not yet tagged). Deploy this ONE zip.
  Migrations 0001-0111 (none new). Folds in everything prior + the tech-stack
  hardening below.

TECH-STACK HARDENING ADDED (this build)
  - noindex: X-Robots-Tag header (next.config.ts) + app/robots.ts disallow all.
    A clinical app must not be search-indexed.
  - Health endpoint: GET /api/health -> {status:'ok', version, time}. Point an
    external uptime monitor (UptimeRobot/Better Uptime/Vercel) at it.
  - Rate limiting: lib/rateLimit.ts (in-memory fixed window). Applied to
    redcap-sync (per admin, 5/min), create-account + reset-password (per IP,
    20/min). Best-effort per serverless instance; swap in KV/Upstash for strict
    global limits (signature is store-agnostic).
  - a11y lint: eslint-plugin-jsx-a11y wired as WARNINGS (0 errors preserved;
    now 244 warnings, ~164 a11y) for the WCAG pass. Deprecated label-has-for off.

  SCAFFOLDED / HANDED OFF (need CLI or Supabase console — not doable in-repo):
  - Typed DB layer LIVE: lib/database.types.ts (30 tables/25 enums/175 funcs) +
    all 3 clients typed <Database>; every query schema-checked at compile time.
    The ~57 adoption mismatches resolved with NO runtime change (casts preserve
    values; ?? undefined only where the SQL param defaults to null). Full verify
    incl. vitest (41 tests) green. gen:types script + docs/DB-TYPES.md included.
  - Production auth email (SMTP): documented in OPS.md go-live checklist. Account
    creation / password reset use Supabase default SMTP (rate-limited/unbranded);
    configure a real provider before real users.

  OPS.md go-live checklist refreshed: CSP + Next.js update marked done; uptime
  monitor + custom SMTP items added.

VERSION
  Unchanged at 1.0.0 (you haven't locked/tagged 1.0 yet). lib/version.ts +
  package.json stay 1.0.0; CHANGELOG 1.0.0 entry updated to include this
  hardening + known-limitations (typed DB, auth email, in-memory rate limit).

VERIFIED
  tsc clean; eslint 0 errors (244 warnings); check:i18n 1730 keys en/da aligned;
  font-stub build compiled (/api/health + /robots.txt in manifest); layout.tsx
  SHA restored (d6901997...); 0 BUILD-STUB remnants.

MIGRATIONS  None new.

  - a11y pass (safe subset): fixed html-has-lang (global-error), removed a
    redundant role, and set aria-role ignoreNonDOM (the 8 aria-role warnings were
    FALSE POSITIVES - custom components with a domain `role` prop). 244->234
    lint warnings, still 0 errors. Form-label association (~118) and modal
    keyboard/dialog handling are deferred to the WCAG/screen-reader pass on
    purpose - quick-hacking them would risk layout breakage or REGRESS real a11y
    to please the linter. See docs/ROADMAP.md 'Accessibility'.

  - Security Advisor follow-up: migration 0112_tighten_anon_studies_and_search_path.sql
    (standalone SQL provided; run in Supabase SQL editor). (a) Revokes anon EXECUTE
    on the 5 study RPCs (create_study/update_study/add_patient_to_study/
    remove_patient_from_study/study_overview) - they were added in 0110 AFTER 0109's
    anon revoke so they still defaulted to PUBLIC (admin-gated internally, so
    defence-in-depth). (b) Pins search_path=public on nrs_to_gas, gas_label,
    audit_event_immutable, approved_goal_set_lineage (+ whoami if present). Verified
    on a throwaway PG16: anon loses EXECUTE on all 5, authenticated/service_role keep
    it, proconfig shows search_path pinned. The large "authenticated can execute
    SECURITY DEFINER" advisor list is BY DESIGN (RPCs self-authorize internally) -
    not actioned. extension_in_public (citext/pg_net) left as-is (low priority,
    risky to move). DASHBOARD: enable Auth leaked-password protection (HaveIBeenPwned)
    - one toggle. DRIFT FOUND: `whoami` exists in the DB but not in migrations -
    investigate/drop or add to a migration.

  - Performance Advisor follow-up: migration 0113_rls_initplan_select_wrap.sql
    (standalone SQL; run in Supabase SQL editor). PERFORMANCE-only (no security
    change). Wraps auth.uid() as (select auth.uid()) in the 11 auth_rls_initplan-
    flagged self-access policies (profile x2, patient, clinician, audit_event x2,
    push_subscription x3, device_push_token x2) so the planner evaluates it once
    per statement instead of per row. Verified on a throwaway PG16 under a non-owner
    role with RLS enforced: per-row visibility + INSERT/DELETE allow/deny identical
    before vs after; pg_policies shows the wrapped expression. current_app_role() in
    profile_self_update left untouched (different lint surface).
    NOT actioned: multiple_permissive_policies (the large list) - consolidating the
    admin/clinician/patient permissive policies into single OR'd policies is a
    security-boundary rewrite with near-zero pre-pilot payoff; deferred to a dedicated
    RLS performance pass (added to ROADMAP). Most rows in that list are also for non-
    real roles (anon/authenticator/dashboard_user/supabase_privileged_role); only the
    `authenticated` rows represent real query cost.
