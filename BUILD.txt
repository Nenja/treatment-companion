Build tag: copy-to-other-side
Generated: 2026-06-02T18:22Z

What changed (vs "face-model-toggle"):
- Face map: two buttons under the map — "Copy right → left" and
  "Copy left → right". Each takes the marks on the source side, mirrors them
  across the midline (posX -> 1 - posX) with the side flipped, and appends them
  as new marks. Originals are kept; center/bilateral marks are not copied. Each
  button disables when its source side has no marks.
- Orientation is patient-perspective (matches the R/L labels and side
  auto-detect): "right" = patient's right = viewer's left.
- No DB change (marks save through the normal path). No migration.

How to confirm THIS build is deployed:
- BUILD.txt at repo root says tag "copy-to-other-side".
- Place marks on one side of the face; the matching "Copy <side> → <side>"
  button becomes enabled and one click mirrors them to the other side.
