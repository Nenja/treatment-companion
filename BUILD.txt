BUILD: simplify-cockpit-20   (goal-calibration forms explain a disabled Save — NO migration)
==========================================================================
MIGRATION: none. DB stays at 0090. Nothing to run in Supabase.

CONTEXT: the audit's "NRS cut-off stepper" item turned out MOOT — the clinician
never enters cut-offs (they're fixed server-side defaults; cutoffError is an
orphaned, unused string). The real friction in that flow was that both
goal-calibration forms simply greyed out Save with no explanation.

DONE — a "what's still needed" helper (mirrors the treatment form) on BOTH
calibration surfaces, shown only once the clinician has started:
- new-goal (RecordGoalForm): lists whichever of these is missing — goal text,
  SMART description, weekly NRS question, starting-point + target, or all five
  outcome levels. Keys under newGoal.
- approve (/clinician/suggestion): same, keys under clinician.approve.

VERIFIED LOCALLY
- tsc --noEmit clean.
- i18n parity: 0 key mismatches, 0 ICU mismatches.
- Production build OK via the font-stub procedure. 60/60 pages.

DEPLOY: no migration; unzip / push / Vercel builds. Page count 60.

** QA **
- Start a new goal or approve a suggestion, leave a required field blank: a
  "Before you can save/approve:" list names exactly what's missing. Fill them
  and the list disappears and Save/Approve enables.

AUDIT STATUS: the cheap/contained "don't make me think" items are now done or
were already handled in the codebase. What remains is judgment-call polish best
driven by a live moderated test (3 per role; patients on phone,
clinicians/therapists on desktop).
