BUILD: simplify-cockpit-11   (#11 therapist modules gated on engagement — NO migration)
==========================================================================
MIGRATION: none. DB stays at 0088. Nothing to run in Supabase.
Batch 11 of cockpit simplification. Backlog + status in HANDOVER §8.

DONE — #11: only show therapist information once a therapist has engaged.
- Cockpit (per goal): ALREADY gated by data — a goal shows the therapist overlay
  (physio ratings) / working-on tag only once a therapist has evaluated that
  goal. No change needed.
- Treatment page: the "Therapist input" button and the physician->therapist
  handoff panel (title + "treatment changed?" toggle + note) now render only when
  therapistHasEngaged = physioAssessments.length > 0 || therapistSuggestionCount
  > 0 (the therapist has evaluated or suggested this cycle). Until then the
  physician sees no therapist UI for the patient.

GRANULARITY NOTE
- The handoff note is a PER-CYCLE field (treatment_handoff), so it activates once
  the therapist engages with the cycle (any goal), not strictly per-goal. A true
  per-goal handoff note would need a schema change (migration). Say the word if
  you want per-goal notes and I'll plan that migration.

VERIFIED LOCALLY
- tsc --noEmit clean.
- Production build OK via the font-stub procedure (restored byte-for-byte, zero
  BUILD-STUB remnants). 60/60 pages.

DEPLOY: no migration; unzip, push, Vercel builds. Page count 60.

** QA **
- A patient with NO therapist activity: treatment page shows no Therapist-input
  button and no handoff panel. After a therapist evaluates or suggests, both
  appear. Cockpit goals only show therapist overlay/working-on for goals the
  therapist evaluated.

BACKLOG REMAINING (HANDOVER §8): #4 muscle->function (parked, draft ready);
per-goal handoff note (migration, only if wanted); adjustment-request status
loop (migration); REDCap / EHR-content decisions.
