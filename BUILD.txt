Build tag: brand-follows-page-width
Generated: 2026-06-02T20:13Z

Single change from the previous build (facemap-revert-and-global-brand);
UI-only, NO migration:
- The global brand strip's content is now centred at the page content
  width (mid / 720px) instead of the viewport's far-left corner. The strip
  still spans the full width (standard app-bar look), but the chevron +
  "Treatment Companion" now sit at the top-left of the content column, so
  they line up with the page below on the main clinician work pages
  (treatment, patient, new-goal, physio patient — all 720px).

Note: pages that use a different content width (history + patient-info are
wider at 1080; the patient-facing pages are 480) may show the brand a touch
off from their content on a wide desktop window. If you want pixel-exact
alignment on every page, the next step is to feed each page's own width to
the strip — say the word and I'll wire it.

How to confirm THIS build is deployed:
- BUILD.txt tag "brand-follows-page-width".
- On the treatment/patient pages, the brand lines up with the left edge of
  the page content instead of the window corner.
