BUILD - next16-upgrade-1   (framework upgrade: Next.js 15.1.9 -> 16.2.7)

WHY
  Your pinned Next.js 15.1.9 was missing several patched security fixes,
  including a critical (CVSS 10.0) remote-code-execution issue. This upgrade
  closes them. Going to 16 (rather than the minimal 15.5.18 patch) keeps you
  current. The two HIGH-severity advisories are now resolved.

WHAT'S IN THIS DROP
  package.json          next 16.2.7, next-intl 4.13.0, @sentry/nextjs 10.58.0
  package-lock.json     regenerated to match (Vercel installs from this)
  proxy.ts              NEW - this is your old middleware.ts, renamed for Next 16

*** ONE MANUAL STEP THE ZIP CANNOT DO FOR YOU ***
  DELETE  middleware.ts  from the repo.
  Next 16 renamed the "middleware" file to "proxy". I've added proxy.ts (same
  logic, function renamed). If you leave BOTH middleware.ts and proxy.ts in the
  repo, Next 16 errors. So: add proxy.ts, then DELETE middleware.ts.
  (In GitHub Desktop: delete the file in your local folder, and the commit will
  record the deletion.)

DEPLOY
  1. Make sure Vercel's Node version is 20 or higher (Next 16 requires it).
     Vercel's default is already 20+, so this is almost certainly fine; just
     confirm under Project Settings -> if you've pinned an older Node, raise it.
  2. GitHub Desktop:
       - copy package.json + package-lock.json into the repo (overwrite)
       - add proxy.ts
       - DELETE middleware.ts
       - commit -> push
  3. Vercel rebuilds (now with Turbopack, Next 16's default builder).

VERIFICATION DONE FOR YOU (sandbox)
  - TypeScript: clean.
  - Production build (Turbopack): success, 109/109 pages, no warnings.
  - i18n parity: 1668 keys aligned. Unit tests: 41/41. E2E spec: type-clean.
  - npm audit: the 2 HIGH advisories are gone (6 left: low/moderate only).

WHAT I CANNOT VERIFY - PLEASE CHECK AFTER DEPLOY (this was a 3-major-version jump)
  1. Re-run the E2E smoke (Actions -> E2E smoke). It logs in, checks the
     signed-out redirect, signs in, and completes a check-in - against the live
     Next 16 build. Green = the critical paths survived the upgrade.
  2. Click through ALL FOUR languages: /, /da, /sv, /nb. Switch language in the
     app. Confirm pages load and translations appear. (next-intl went to v4 and
     the middleware->proxy rename both touch language routing - this is the main
     thing to eyeball, and the E2E only covers English.)
  3. Log in, move between pages, refresh. Confirm you stay logged in (proxy.ts
     handles the session refresh) and that logging out still lands on /login.
  4. Confirm a push notification still registers on your phone.

  If anything is off, the zero-risk fallback is Next 15.5.18 (same security fix,
  no other changes) - tell me and I'll prepare it.

NOTES
  - Two leftover low/moderate audit items (@supabase/auth-js, postcss) are
    separate dependency-currency follow-ups, not blockers.
  - Your CI (ci.yml) keeps working: it type-checks, runs tests, and builds on
    Next 16 the same way, and replays migrations unchanged.
