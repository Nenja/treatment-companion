BUILD: simplify-cockpit-84 — ADMIN RESEARCH-CONSENT PURGE QUEUE. Completes the cockpit-83
withdrawal lifecycle: an admin can now see who withdrew research consent and confirm deletion.
NO NEW MIGRATION (pure app code on top of 0098).

DISCARD 21-83 (superseded). Keep this zip.

DB STATE — no new migration in this build. Still run any PENDING ones in order:
  • 0090, 0095, 0096, 0097, 0098  (paste schema_audit.sql to see which are still MISSING)
  0098 (cockpit-83) is the one that matters for this feature — the purge queue calls its
  confirm_research_purge RPC and reads its research_consent_withdrawn_at / _purged_at columns.

WHAT CHANGED:
  • /clinician/admin gains a "Research consent — pending deletion" section: a queue of patients
      with research_consent_withdrawn_at set and research_consent_purged_at null, each with a
      Confirm-deletion button (guarded by a confirm dialog).
  • Confirming calls confirm_research_purge (admin-only, migration 0098) → stamps
      research_consent_purged_at and removes the row from the queue. It records the admin's
      authorisation to delete; the actual REDCap delete will be done by the export job.
  • No schema change: the queue reads the patient table via the admin's existing patient_admin_all
      RLS (migration 0037). New hooks useResearchPurgeQueue / useConfirmResearchPurge in
      lib/supabase/admin.ts. New admin.purge* i18n (en + da; Danish is first-pass).

NOTE: the queue is forward-looking — it stays EMPTY until the REDCap export exists and a
  consented patient withdraws. It's shipped now so the lifecycle is complete and callable.

CANNOT BE VERIFIED HERE (your QA):
  • Dark-mode contrast of the amber queue card.
  • Native-Danish review of the admin.purge* strings.
  • Live admin RLS read of the queue (Method-D covers the RPC logic, not your live policies).

BUILD VERIFICATION (this end): next build → 62/62; tsc --noEmit clean; i18n parity en/da equal
  (only _meta differs); font-stub restored.

DEPLOY: unzip → GitHub → Vercel. No SQL to run for this build (beyond any still-pending 0090–0098).
  Hard-refresh Firefox (Ctrl+Shift+R) if the live site looks stale.
