BUILD - test-harness-2   (CUMULATIVE)
The test harness was already in place (Vitest, 23 tests, wired into CI). This
batch EXTENDS it with tests for the EHR-export text builder — the text you paste
into the medical record. 35 tests now pass.

>>> CUMULATIVE — one drop-in deploys everything. <<<

WHAT'S NEW THIS BATCH
  tests/ehrExport.test.ts   (+12 tests for buildEhrExport)
That is the only new file vs the consolidate-charts-1 bundle. If you've already
deployed test-harness-1 + consolidate-charts-1, you can drop just this one file.

WHAT THE NEW TESTS CHECK (buildEhrExport)
  - the header switches between the "injected" and neutral wording by modality
  - treatment summary line (product · total units · dilution · guidance)
  - the dose RECONCILIATION line appears only when per-injection doses don't add
    up to the recorded total
  - body-muscle injections list before face marks
  - goal lines: GAS peak/end levels + achieved anchor + wearing-off week; NRS
    goals (lower-is-better picks the lowest score as "best") with the
    baseline/target line; sustained benefit vs wearing-off; the no-ratings line

CARRIED (cumulative, unchanged from before)
  - the full Vitest harness: vitest.config.ts, tests/*.test.ts (5),
    package.json, package-lock.json, .github/workflows/ci.yml
  - chart consolidation: app/[locale]/clinician/patient/page.tsx
  - guarded migration 0104

RUNS IN CI: every push/PR runs `npm run test` in the verify job — so the suite
runs for you even though you can't run it locally.

PACKAGE.JSON / LOCK: included again (same as test-harness-1). The only delta is
the vitest devDependency + the test scripts; runtime `dependencies` are
untouched, so the Vercel build is unaffected. If your committed package.json has
drifted, apply just that diff instead of overwriting.

STILL TO RUN IN SUPABASE (unchanged): 0103, 0104 (re-run; guarded), 0105, 0106.

DEPLOY: copy files into the repo (keep folders) -> Commit -> Push.
Do NOT run npm install yourself.
