BUILD - e2e-smoke-1   (CUMULATIVE)
Added a Playwright end-to-end smoke (login + weekly check-in) as a dev-facing
scaffold. No app behaviour changes in this batch.

>>> CUMULATIVE — this one drop-in also carries the two previous batches
>>> (consent-consolidation-1 and suggestion-approve-redesign-1) and the
>>> migration 0107. Dropping this zip deploys everything from this session. <<<

WHAT'S IN THIS DROP
  This batch (E2E scaffold — does NOT affect the running app):
    playwright.config.ts
    e2e/smoke.spec.ts
    e2e/README.md
    e2e/.gitignore
    .github/workflows/e2e.yml          (MANUAL trigger only — never gates pushes)
    tsconfig.json                      (only adds "e2e" to exclude)
  Carried from consent-consolidation-1:
    components/clinician/ClinicianVideoModal.tsx   (now the Consent panel)
    components/clinician/BackgroundCard.tsx        (consent rows removed)
    components/clinician/PatientActionRow.tsx      (Video icon -> shield-check)
  Carried from suggestion-approve-redesign-1:
    supabase/migrations/0107_combine_suggestion_into_goal.sql   (MIGRATION)
    lib/supabase/clinicianPatient.ts               (combine hook)
    app/[locale]/clinician/suggestion/page.tsx     (Approve / Set aside)
  Touched by both feature batches:
    app/[locale]/clinician/patient/page.tsx
    messages/en|da|sv|nb.json

DEPLOY — IN ORDER (same as before; nothing new to run for the E2E part)
  1. Supabase SQL editor: run (if not already done from a prior drop)
         supabase/migrations/0107_combine_suggestion_into_goal.sql   (idempotent)
  2. GitHub Desktop: drop all files from this zip into the repo, same paths.
     DO NOT overwrite root package.json / package-lock.json (untouched here).
  3. Commit + push. Vercel auto-builds. The e2e/ files and the manual workflow
     have no effect on the build or the running app.

USING THE E2E SMOKE (optional, for you or the incoming developer)
  One-time:
     npm install -D @playwright/test
     npx playwright install chromium
  Run locally (starts the dev server for you; needs .env.local):
     npx playwright test
  Or against the live site:
     E2E_BASE_URL=https://treatment-companion.vercel.app \
     E2E_PATIENT_EMAIL=test1@example.com E2E_PATIENT_PASSWORD=... \
     npx playwright test
  Full details + preconditions: e2e/README.md

VERIFICATION DONE FOR YOU (sandbox)
  - App build: 110/110 pages, exit 0.  TypeScript: clean.  Tests: 41/41.
  - i18n parity: 1668 keys, en/da aligned.
  - The e2e spec + config TYPE-CHECK clean against @playwright/test.
  - (Migration 0107 was verified on a throwaway Postgres earlier: focused
     harness + all four guards + full 0001->0107 chain, 0 failures.)

HONEST LIMITS
  - I could NOT run the browser tests here (no browser, no live app). The two
    signed-out tests are robust; the two signed-in tests are env-gated (they
    SKIP without credentials) and may need a small selector tweak on the first
    real run — that first green run is the real acceptance. See e2e/README.md.
  - As always: I can't see rendered screens, real device behaviour, or live
    RLS/auth. sv/nb strings (from the carried batches) are first-pass.

ROADMAP
  This was the last item on your audit list. All five are now delivered:
  test harness, privacy/DPIA drafts, native-push runbook, sv/nb reminder
  localization, and this E2E smoke.
