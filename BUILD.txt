BUILD - staging-ci-gate-1   (deploy-on-green + staging guide)

WHAT THIS IS
  Cumulative repo snapshot. Docs + ONE inert CI workflow on top of
  rls-denial-tests-1 / studies-and-fixes-1. NO app code change, NO new
  migration. Stack unchanged: Next.js 16.2.7 / Supabase (Postgres 16).
  Migrations still 0001-0110. Nothing to run for this increment.

WHAT CHANGED THIS INCREMENT
  Deploy-on-green (.github/workflows/deploy.yml)
    - Deploys production via the Vercel CLI ONLY after the CI workflow finishes
      successfully on `main`. Makes CI a real gate instead of a signal: a red
      build can no longer go live, even with direct-to-main (zip-upload)
      commits.
    - INERT until activated: a guard step skips the whole deploy unless
      VERCEL_TOKEN is present, so simply having this file in the repo does
      nothing until you wire it up.

  Staging + CI-gate guide (docs/STAGING-AND-CI-GATE.md)
    - Click-path to activate the gate, and to stand up a staging Supabase
      project + a Vercel Preview environment pointed at it, so changes are
      tested off real patient data before production.
    - OPS.md section 4 now points to it.

  Rationale (captured in the guide + HANDOVER): requiring pull requests /
  branch protection is the right END STATE but breaks your current web
  zip-upload-to-main flow, so it is deferred to the incoming developer.
  Deploy-on-green is the friction-free interim that still gates production.

VERIFIED HERE
  - Both workflow YAML files parse (ci.yml, deploy.yml).
  - No app code changed; prior build/tsc remain valid.

YOUR ACTIONS (dashboard) - to ACTIVATE (full steps in the guide)
  Part A - deploy-on-green:
    1. Vercel -> Project -> Settings -> Git: turn OFF automatic production
       deployments for `main` (else you get double deploys).
    2. Add GitHub repo secrets: VERCEL_TOKEN, VERCEL_ORG_ID, VERCEL_PROJECT_ID.
    3. Push a small change; confirm in GitHub Actions that Deploy runs only
       after CI is green.
  Part B - staging:
    1. Create a second Supabase project (…-staging); run all migrations in it
       once. New rule: run a migration in staging first, then production.
    2. Vercel env vars: set the Preview scope of NEXT_PUBLIC_SUPABASE_URL /
       _ANON_KEY to the staging project (keep Production on the real one).
    3. Upload to a `staging` branch to get a Preview URL on staging data.

STILL OPEN (from the critical path we agreed)
  - Tested restore (Supabase Pro + PITR, restore once) - highest data-loss
    risk, still your action.
  - Native-Danish clinical-string review.
  - Decision still pending from rls-denial-tests-1: care-team notes are
    patient-readable since migration 0096 (see that section / HANDOVER 5.14).

MIGRATIONS
  Nothing new. If 0110_studies.sql is not yet applied, run it (copy in
  outputs). Otherwise no SQL to run.

NOTE ON THIS ZIP
  Full repo minus node_modules, .next, build artifacts, and .git.
