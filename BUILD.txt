BUILD TAG: batch-c
=====================================================================

WHAT CHANGED — minor polish (from the page-by-page review)
---------------------------------------------------------------------
Small, low-risk fixes. NO database / RPC / migration change.

1. Patient home — "View graph" only appears when there's data.
   Previously the graph button showed on every goal card, including a
   brand-new goal with no check-ins yet, where it opened an empty chart.
   Now the button is hidden until the goal has at least one check-in
   rating, then it appears.

2. Clinician entry screen — fixed a duplicated label. The visit-code
   input's field label repeated the page heading (both "Enter visit
   code"). The field now has its own label, "Visit code"
   (da "Besøgskode").

3. Stale code comments corrected (comments only, no behaviour change):
   - the check-in "step plan" doc block now accurately describes the
     wizard (per-goal NRS/GAS rating steps, optional video, training
     days, then comment + summary) instead of the old "5 GAS anchors
     for every goal";
   - physio/patient's "SLICE 1 - placeholder" header replaced with a
     description of the real read+suggest view it is now;
   - the clinician/patient comment that said GAS capture "ships in the
     check-in slice" (it shipped long ago) trimmed.

(The new-goal createGas.isPending submit-label bug noted in the review
was already fixed in batch-a.)


DEPLOY STEPS
---------------------------------------------------------------------
1. Push the repo to GitHub -> Vercel as usual. No env changes.
2. DB: nothing new. Migrations still through 0067. If you have not yet
   run 0067 (from batch-a, for GAS suggestion-approval), run it now:
       supabase/migrations/0067_approve_suggestion_gas.sql


HOW TO CONFIRM
---------------------------------------------------------------------
- Patient home: a goal with no check-ins yet shows NO "View graph"
  button; after the first check-in it appears.
- Clinician entry screen: under the "Enter visit code" heading, the
  field label reads "Visit code" (not a repeat of the heading).


VISUAL QA FLAGS (could not be rendered in the build sandbox)
---------------------------------------------------------------------
[ ] Confirm the View-graph button hides/reappears as described, in both
    en and da.


BUILD VERIFICATION
---------------------------------------------------------------------
- next build: exit 0, "Generating static pages (58/58)".
- npx tsc --noEmit: exit 0.
- en/da translation parity: 1020 == 1020 keys, no diffs.
- Migrations unchanged (through 0067).
