Build tag: facemap-copy-clear
Generated: 2026-06-02T18:48Z

What changed (vs "facemap-a11y-fixes"). Face map only; NO migration:
- Copy-to-other-side is now allowed ONCE. After a copy, both copy buttons
  disable and a short note appears ("Copied. Clear the marks to copy again.").
  Clearing the marks re-enables copying.
- Removed the four-box summary row under the map (Total / Left / Right / Marks).
  (The cycle total is still computed by the treatment page; only the on-map
  mini-summary is gone.)
- New "Clear marks" button (under the copy buttons): destructive, so it uses a
  two-step inline confirm — tap "Clear marks" → "Clear all? [Clear all][Cancel]".
  Only shown when there are marks. Clearing also resets the copy-once lock.

How to confirm THIS build is deployed:
- BUILD.txt tag "facemap-copy-clear".
- No summary boxes beneath the face; a "Clear marks" button is present; using a
  "Copy <side> → <side>" button once disables both copy buttons until cleared.
